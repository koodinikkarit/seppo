// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"database/sql"
	"fmt"
	"reflect"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// SRPassivatedVariationVersion is an object representing the database table.
type SRPassivatedVariationVersion struct {
	ID                 uint64 `boil:"id" json:"id" toml:"id" yaml:"id"`
	SRID               uint64 `boil:"sr_id" json:"sr_id" toml:"sr_id" yaml:"sr_id"`
	VariationVersionID uint64 `boil:"variation_version_id" json:"variation_version_id" toml:"variation_version_id" yaml:"variation_version_id"`

	R *srPassivatedVariationVersionR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L srPassivatedVariationVersionL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var SRPassivatedVariationVersionColumns = struct {
	ID                 string
	SRID               string
	VariationVersionID string
}{
	ID:                 "id",
	SRID:               "sr_id",
	VariationVersionID: "variation_version_id",
}

// srPassivatedVariationVersionR is where relationships are stored.
type srPassivatedVariationVersionR struct {
	SR               *SynchronizationRaport
	VariationVersion *VariationVersion
}

// srPassivatedVariationVersionL is where Load methods for each relationship are stored.
type srPassivatedVariationVersionL struct{}

var (
	srPassivatedVariationVersionColumns               = []string{"id", "sr_id", "variation_version_id"}
	srPassivatedVariationVersionColumnsWithoutDefault = []string{"sr_id", "variation_version_id"}
	srPassivatedVariationVersionColumnsWithDefault    = []string{"id"}
	srPassivatedVariationVersionPrimaryKeyColumns     = []string{"id"}
)

type (
	// SRPassivatedVariationVersionSlice is an alias for a slice of pointers to SRPassivatedVariationVersion.
	// This should generally be used opposed to []SRPassivatedVariationVersion.
	SRPassivatedVariationVersionSlice []*SRPassivatedVariationVersion
	// SRPassivatedVariationVersionHook is the signature for custom SRPassivatedVariationVersion hook methods
	SRPassivatedVariationVersionHook func(boil.Executor, *SRPassivatedVariationVersion) error

	srPassivatedVariationVersionQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	srPassivatedVariationVersionType                 = reflect.TypeOf(&SRPassivatedVariationVersion{})
	srPassivatedVariationVersionMapping              = queries.MakeStructMapping(srPassivatedVariationVersionType)
	srPassivatedVariationVersionPrimaryKeyMapping, _ = queries.BindMapping(srPassivatedVariationVersionType, srPassivatedVariationVersionMapping, srPassivatedVariationVersionPrimaryKeyColumns)
	srPassivatedVariationVersionInsertCacheMut       sync.RWMutex
	srPassivatedVariationVersionInsertCache          = make(map[string]insertCache)
	srPassivatedVariationVersionUpdateCacheMut       sync.RWMutex
	srPassivatedVariationVersionUpdateCache          = make(map[string]updateCache)
	srPassivatedVariationVersionUpsertCacheMut       sync.RWMutex
	srPassivatedVariationVersionUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force bytes in case of primary key column that uses []byte (for relationship compares)
	_ = bytes.MinRead
)
var srPassivatedVariationVersionBeforeInsertHooks []SRPassivatedVariationVersionHook
var srPassivatedVariationVersionBeforeUpdateHooks []SRPassivatedVariationVersionHook
var srPassivatedVariationVersionBeforeDeleteHooks []SRPassivatedVariationVersionHook
var srPassivatedVariationVersionBeforeUpsertHooks []SRPassivatedVariationVersionHook

var srPassivatedVariationVersionAfterInsertHooks []SRPassivatedVariationVersionHook
var srPassivatedVariationVersionAfterSelectHooks []SRPassivatedVariationVersionHook
var srPassivatedVariationVersionAfterUpdateHooks []SRPassivatedVariationVersionHook
var srPassivatedVariationVersionAfterDeleteHooks []SRPassivatedVariationVersionHook
var srPassivatedVariationVersionAfterUpsertHooks []SRPassivatedVariationVersionHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *SRPassivatedVariationVersion) doBeforeInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range srPassivatedVariationVersionBeforeInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *SRPassivatedVariationVersion) doBeforeUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range srPassivatedVariationVersionBeforeUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *SRPassivatedVariationVersion) doBeforeDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range srPassivatedVariationVersionBeforeDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *SRPassivatedVariationVersion) doBeforeUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range srPassivatedVariationVersionBeforeUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *SRPassivatedVariationVersion) doAfterInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range srPassivatedVariationVersionAfterInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *SRPassivatedVariationVersion) doAfterSelectHooks(exec boil.Executor) (err error) {
	for _, hook := range srPassivatedVariationVersionAfterSelectHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *SRPassivatedVariationVersion) doAfterUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range srPassivatedVariationVersionAfterUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *SRPassivatedVariationVersion) doAfterDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range srPassivatedVariationVersionAfterDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *SRPassivatedVariationVersion) doAfterUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range srPassivatedVariationVersionAfterUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddSRPassivatedVariationVersionHook registers your hook function for all future operations.
func AddSRPassivatedVariationVersionHook(hookPoint boil.HookPoint, srPassivatedVariationVersionHook SRPassivatedVariationVersionHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		srPassivatedVariationVersionBeforeInsertHooks = append(srPassivatedVariationVersionBeforeInsertHooks, srPassivatedVariationVersionHook)
	case boil.BeforeUpdateHook:
		srPassivatedVariationVersionBeforeUpdateHooks = append(srPassivatedVariationVersionBeforeUpdateHooks, srPassivatedVariationVersionHook)
	case boil.BeforeDeleteHook:
		srPassivatedVariationVersionBeforeDeleteHooks = append(srPassivatedVariationVersionBeforeDeleteHooks, srPassivatedVariationVersionHook)
	case boil.BeforeUpsertHook:
		srPassivatedVariationVersionBeforeUpsertHooks = append(srPassivatedVariationVersionBeforeUpsertHooks, srPassivatedVariationVersionHook)
	case boil.AfterInsertHook:
		srPassivatedVariationVersionAfterInsertHooks = append(srPassivatedVariationVersionAfterInsertHooks, srPassivatedVariationVersionHook)
	case boil.AfterSelectHook:
		srPassivatedVariationVersionAfterSelectHooks = append(srPassivatedVariationVersionAfterSelectHooks, srPassivatedVariationVersionHook)
	case boil.AfterUpdateHook:
		srPassivatedVariationVersionAfterUpdateHooks = append(srPassivatedVariationVersionAfterUpdateHooks, srPassivatedVariationVersionHook)
	case boil.AfterDeleteHook:
		srPassivatedVariationVersionAfterDeleteHooks = append(srPassivatedVariationVersionAfterDeleteHooks, srPassivatedVariationVersionHook)
	case boil.AfterUpsertHook:
		srPassivatedVariationVersionAfterUpsertHooks = append(srPassivatedVariationVersionAfterUpsertHooks, srPassivatedVariationVersionHook)
	}
}

// OneP returns a single srPassivatedVariationVersion record from the query, and panics on error.
func (q srPassivatedVariationVersionQuery) OneP() *SRPassivatedVariationVersion {
	o, err := q.One()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single srPassivatedVariationVersion record from the query.
func (q srPassivatedVariationVersionQuery) One() (*SRPassivatedVariationVersion, error) {
	o := &SRPassivatedVariationVersion{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for sr_passivated_variation_versions")
	}

	if err := o.doAfterSelectHooks(queries.GetExecutor(q.Query)); err != nil {
		return o, err
	}

	return o, nil
}

// AllP returns all SRPassivatedVariationVersion records from the query, and panics on error.
func (q srPassivatedVariationVersionQuery) AllP() SRPassivatedVariationVersionSlice {
	o, err := q.All()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all SRPassivatedVariationVersion records from the query.
func (q srPassivatedVariationVersionQuery) All() (SRPassivatedVariationVersionSlice, error) {
	var o []*SRPassivatedVariationVersion

	err := q.Bind(&o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to SRPassivatedVariationVersion slice")
	}

	if len(srPassivatedVariationVersionAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(queries.GetExecutor(q.Query)); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountP returns the count of all SRPassivatedVariationVersion records in the query, and panics on error.
func (q srPassivatedVariationVersionQuery) CountP() int64 {
	c, err := q.Count()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all SRPassivatedVariationVersion records in the query.
func (q srPassivatedVariationVersionQuery) Count() (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count sr_passivated_variation_versions rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table, and panics on error.
func (q srPassivatedVariationVersionQuery) ExistsP() bool {
	e, err := q.Exists()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q srPassivatedVariationVersionQuery) Exists() (bool, error) {
	var count int64

	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if sr_passivated_variation_versions exists")
	}

	return count > 0, nil
}

// SRG pointed to by the foreign key.
func (o *SRPassivatedVariationVersion) SRG(mods ...qm.QueryMod) synchronizationRaportQuery {
	return o.SR(boil.GetDB(), mods...)
}

// SR pointed to by the foreign key.
func (o *SRPassivatedVariationVersion) SR(exec boil.Executor, mods ...qm.QueryMod) synchronizationRaportQuery {
	queryMods := []qm.QueryMod{
		qm.Where("id=?", o.SRID),
	}

	queryMods = append(queryMods, mods...)

	query := SynchronizationRaports(exec, queryMods...)
	queries.SetFrom(query.Query, "`synchronization_raports`")

	return query
}

// VariationVersionG pointed to by the foreign key.
func (o *SRPassivatedVariationVersion) VariationVersionG(mods ...qm.QueryMod) variationVersionQuery {
	return o.VariationVersion(boil.GetDB(), mods...)
}

// VariationVersion pointed to by the foreign key.
func (o *SRPassivatedVariationVersion) VariationVersion(exec boil.Executor, mods ...qm.QueryMod) variationVersionQuery {
	queryMods := []qm.QueryMod{
		qm.Where("id=?", o.VariationVersionID),
	}

	queryMods = append(queryMods, mods...)

	query := VariationVersions(exec, queryMods...)
	queries.SetFrom(query.Query, "`variation_versions`")

	return query
} // LoadSR allows an eager lookup of values, cached into the
// loaded structs of the objects.
func (srPassivatedVariationVersionL) LoadSR(e boil.Executor, singular bool, maybeSRPassivatedVariationVersion interface{}) error {
	var slice []*SRPassivatedVariationVersion
	var object *SRPassivatedVariationVersion

	count := 1
	if singular {
		object = maybeSRPassivatedVariationVersion.(*SRPassivatedVariationVersion)
	} else {
		slice = *maybeSRPassivatedVariationVersion.(*[]*SRPassivatedVariationVersion)
		count = len(slice)
	}

	args := make([]interface{}, count)
	if singular {
		if object.R == nil {
			object.R = &srPassivatedVariationVersionR{}
		}
		args[0] = object.SRID
	} else {
		for i, obj := range slice {
			if obj.R == nil {
				obj.R = &srPassivatedVariationVersionR{}
			}
			args[i] = obj.SRID
		}
	}

	query := fmt.Sprintf(
		"select * from `synchronization_raports` where `id` in (%s)",
		strmangle.Placeholders(dialect.IndexPlaceholders, count, 1, 1),
	)

	if boil.DebugMode {
		fmt.Fprintf(boil.DebugWriter, "%s\n%v\n", query, args)
	}

	results, err := e.Query(query, args...)
	if err != nil {
		return errors.Wrap(err, "failed to eager load SynchronizationRaport")
	}
	defer results.Close()

	var resultSlice []*SynchronizationRaport
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice SynchronizationRaport")
	}

	if len(srPassivatedVariationVersionAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		object.R.SR = resultSlice[0]
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.SRID == foreign.ID {
				local.R.SR = foreign
				break
			}
		}
	}

	return nil
}

// LoadVariationVersion allows an eager lookup of values, cached into the
// loaded structs of the objects.
func (srPassivatedVariationVersionL) LoadVariationVersion(e boil.Executor, singular bool, maybeSRPassivatedVariationVersion interface{}) error {
	var slice []*SRPassivatedVariationVersion
	var object *SRPassivatedVariationVersion

	count := 1
	if singular {
		object = maybeSRPassivatedVariationVersion.(*SRPassivatedVariationVersion)
	} else {
		slice = *maybeSRPassivatedVariationVersion.(*[]*SRPassivatedVariationVersion)
		count = len(slice)
	}

	args := make([]interface{}, count)
	if singular {
		if object.R == nil {
			object.R = &srPassivatedVariationVersionR{}
		}
		args[0] = object.VariationVersionID
	} else {
		for i, obj := range slice {
			if obj.R == nil {
				obj.R = &srPassivatedVariationVersionR{}
			}
			args[i] = obj.VariationVersionID
		}
	}

	query := fmt.Sprintf(
		"select * from `variation_versions` where `id` in (%s)",
		strmangle.Placeholders(dialect.IndexPlaceholders, count, 1, 1),
	)

	if boil.DebugMode {
		fmt.Fprintf(boil.DebugWriter, "%s\n%v\n", query, args)
	}

	results, err := e.Query(query, args...)
	if err != nil {
		return errors.Wrap(err, "failed to eager load VariationVersion")
	}
	defer results.Close()

	var resultSlice []*VariationVersion
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice VariationVersion")
	}

	if len(srPassivatedVariationVersionAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		object.R.VariationVersion = resultSlice[0]
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.VariationVersionID == foreign.ID {
				local.R.VariationVersion = foreign
				break
			}
		}
	}

	return nil
}

// SetSRG of the sr_passivated_variation_version to the related item.
// Sets o.R.SR to related.
// Adds o to related.R.SRSRPassivatedVariationVersions.
// Uses the global database handle.
func (o *SRPassivatedVariationVersion) SetSRG(insert bool, related *SynchronizationRaport) error {
	return o.SetSR(boil.GetDB(), insert, related)
}

// SetSRP of the sr_passivated_variation_version to the related item.
// Sets o.R.SR to related.
// Adds o to related.R.SRSRPassivatedVariationVersions.
// Panics on error.
func (o *SRPassivatedVariationVersion) SetSRP(exec boil.Executor, insert bool, related *SynchronizationRaport) {
	if err := o.SetSR(exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetSRGP of the sr_passivated_variation_version to the related item.
// Sets o.R.SR to related.
// Adds o to related.R.SRSRPassivatedVariationVersions.
// Uses the global database handle and panics on error.
func (o *SRPassivatedVariationVersion) SetSRGP(insert bool, related *SynchronizationRaport) {
	if err := o.SetSR(boil.GetDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetSR of the sr_passivated_variation_version to the related item.
// Sets o.R.SR to related.
// Adds o to related.R.SRSRPassivatedVariationVersions.
func (o *SRPassivatedVariationVersion) SetSR(exec boil.Executor, insert bool, related *SynchronizationRaport) error {
	var err error
	if insert {
		if err = related.Insert(exec); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `sr_passivated_variation_versions` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"sr_id"}),
		strmangle.WhereClause("`", "`", 0, srPassivatedVariationVersionPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.SRID = related.ID

	if o.R == nil {
		o.R = &srPassivatedVariationVersionR{
			SR: related,
		}
	} else {
		o.R.SR = related
	}

	if related.R == nil {
		related.R = &synchronizationRaportR{
			SRSRPassivatedVariationVersions: SRPassivatedVariationVersionSlice{o},
		}
	} else {
		related.R.SRSRPassivatedVariationVersions = append(related.R.SRSRPassivatedVariationVersions, o)
	}

	return nil
}

// SetVariationVersionG of the sr_passivated_variation_version to the related item.
// Sets o.R.VariationVersion to related.
// Adds o to related.R.SRPassivatedVariationVersions.
// Uses the global database handle.
func (o *SRPassivatedVariationVersion) SetVariationVersionG(insert bool, related *VariationVersion) error {
	return o.SetVariationVersion(boil.GetDB(), insert, related)
}

// SetVariationVersionP of the sr_passivated_variation_version to the related item.
// Sets o.R.VariationVersion to related.
// Adds o to related.R.SRPassivatedVariationVersions.
// Panics on error.
func (o *SRPassivatedVariationVersion) SetVariationVersionP(exec boil.Executor, insert bool, related *VariationVersion) {
	if err := o.SetVariationVersion(exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetVariationVersionGP of the sr_passivated_variation_version to the related item.
// Sets o.R.VariationVersion to related.
// Adds o to related.R.SRPassivatedVariationVersions.
// Uses the global database handle and panics on error.
func (o *SRPassivatedVariationVersion) SetVariationVersionGP(insert bool, related *VariationVersion) {
	if err := o.SetVariationVersion(boil.GetDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetVariationVersion of the sr_passivated_variation_version to the related item.
// Sets o.R.VariationVersion to related.
// Adds o to related.R.SRPassivatedVariationVersions.
func (o *SRPassivatedVariationVersion) SetVariationVersion(exec boil.Executor, insert bool, related *VariationVersion) error {
	var err error
	if insert {
		if err = related.Insert(exec); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `sr_passivated_variation_versions` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"variation_version_id"}),
		strmangle.WhereClause("`", "`", 0, srPassivatedVariationVersionPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.VariationVersionID = related.ID

	if o.R == nil {
		o.R = &srPassivatedVariationVersionR{
			VariationVersion: related,
		}
	} else {
		o.R.VariationVersion = related
	}

	if related.R == nil {
		related.R = &variationVersionR{
			SRPassivatedVariationVersions: SRPassivatedVariationVersionSlice{o},
		}
	} else {
		related.R.SRPassivatedVariationVersions = append(related.R.SRPassivatedVariationVersions, o)
	}

	return nil
}

// SRPassivatedVariationVersionsG retrieves all records.
func SRPassivatedVariationVersionsG(mods ...qm.QueryMod) srPassivatedVariationVersionQuery {
	return SRPassivatedVariationVersions(boil.GetDB(), mods...)
}

// SRPassivatedVariationVersions retrieves all the records using an executor.
func SRPassivatedVariationVersions(exec boil.Executor, mods ...qm.QueryMod) srPassivatedVariationVersionQuery {
	mods = append(mods, qm.From("`sr_passivated_variation_versions`"))
	return srPassivatedVariationVersionQuery{NewQuery(exec, mods...)}
}

// FindSRPassivatedVariationVersionG retrieves a single record by ID.
func FindSRPassivatedVariationVersionG(id uint64, selectCols ...string) (*SRPassivatedVariationVersion, error) {
	return FindSRPassivatedVariationVersion(boil.GetDB(), id, selectCols...)
}

// FindSRPassivatedVariationVersionGP retrieves a single record by ID, and panics on error.
func FindSRPassivatedVariationVersionGP(id uint64, selectCols ...string) *SRPassivatedVariationVersion {
	retobj, err := FindSRPassivatedVariationVersion(boil.GetDB(), id, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindSRPassivatedVariationVersion retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindSRPassivatedVariationVersion(exec boil.Executor, id uint64, selectCols ...string) (*SRPassivatedVariationVersion, error) {
	srPassivatedVariationVersionObj := &SRPassivatedVariationVersion{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `sr_passivated_variation_versions` where `id`=?", sel,
	)

	q := queries.Raw(exec, query, id)

	err := q.Bind(srPassivatedVariationVersionObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from sr_passivated_variation_versions")
	}

	return srPassivatedVariationVersionObj, nil
}

// FindSRPassivatedVariationVersionP retrieves a single record by ID with an executor, and panics on error.
func FindSRPassivatedVariationVersionP(exec boil.Executor, id uint64, selectCols ...string) *SRPassivatedVariationVersion {
	retobj, err := FindSRPassivatedVariationVersion(exec, id, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *SRPassivatedVariationVersion) InsertG(whitelist ...string) error {
	return o.Insert(boil.GetDB(), whitelist...)
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *SRPassivatedVariationVersion) InsertGP(whitelist ...string) {
	if err := o.Insert(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *SRPassivatedVariationVersion) InsertP(exec boil.Executor, whitelist ...string) {
	if err := o.Insert(exec, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// Whitelist behavior: If a whitelist is provided, only those columns supplied are inserted
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns without a default value are included (i.e. name, age)
// - All columns with a default, but non-zero are included (i.e. health = 75)
func (o *SRPassivatedVariationVersion) Insert(exec boil.Executor, whitelist ...string) error {
	if o == nil {
		return errors.New("models: no sr_passivated_variation_versions provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(srPassivatedVariationVersionColumnsWithDefault, o)

	key := makeCacheKey(whitelist, nzDefaults)
	srPassivatedVariationVersionInsertCacheMut.RLock()
	cache, cached := srPassivatedVariationVersionInsertCache[key]
	srPassivatedVariationVersionInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := strmangle.InsertColumnSet(
			srPassivatedVariationVersionColumns,
			srPassivatedVariationVersionColumnsWithDefault,
			srPassivatedVariationVersionColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)

		cache.valueMapping, err = queries.BindMapping(srPassivatedVariationVersionType, srPassivatedVariationVersionMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(srPassivatedVariationVersionType, srPassivatedVariationVersionMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `sr_passivated_variation_versions` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.IndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `sr_passivated_variation_versions` () VALUES ()"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `sr_passivated_variation_versions` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, srPassivatedVariationVersionPrimaryKeyColumns))
		}

		if len(wl) != 0 {
			cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.Exec(cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into sr_passivated_variation_versions")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == srPassivatedVariationVersionMapping["ID"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, identifierCols...)
	}

	err = exec.QueryRow(cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for sr_passivated_variation_versions")
	}

CacheNoHooks:
	if !cached {
		srPassivatedVariationVersionInsertCacheMut.Lock()
		srPassivatedVariationVersionInsertCache[key] = cache
		srPassivatedVariationVersionInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(exec)
}

// UpdateG a single SRPassivatedVariationVersion record. See Update for
// whitelist behavior description.
func (o *SRPassivatedVariationVersion) UpdateG(whitelist ...string) error {
	return o.Update(boil.GetDB(), whitelist...)
}

// UpdateGP a single SRPassivatedVariationVersion record.
// UpdateGP takes a whitelist of column names that should be updated.
// Panics on error. See Update for whitelist behavior description.
func (o *SRPassivatedVariationVersion) UpdateGP(whitelist ...string) {
	if err := o.Update(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateP uses an executor to update the SRPassivatedVariationVersion, and panics on error.
// See Update for whitelist behavior description.
func (o *SRPassivatedVariationVersion) UpdateP(exec boil.Executor, whitelist ...string) {
	err := o.Update(exec, whitelist...)
	if err != nil {
		panic(boil.WrapErr(err))
	}
}

// Update uses an executor to update the SRPassivatedVariationVersion.
// Whitelist behavior: If a whitelist is provided, only the columns given are updated.
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns are inferred to start with
// - All primary keys are subtracted from this set
// Update does not automatically update the record in case of default values. Use .Reload()
// to refresh the records.
func (o *SRPassivatedVariationVersion) Update(exec boil.Executor, whitelist ...string) error {
	var err error
	if err = o.doBeforeUpdateHooks(exec); err != nil {
		return err
	}
	key := makeCacheKey(whitelist, nil)
	srPassivatedVariationVersionUpdateCacheMut.RLock()
	cache, cached := srPassivatedVariationVersionUpdateCache[key]
	srPassivatedVariationVersionUpdateCacheMut.RUnlock()

	if !cached {
		wl := strmangle.UpdateColumnSet(
			srPassivatedVariationVersionColumns,
			srPassivatedVariationVersionPrimaryKeyColumns,
			whitelist,
		)

		if len(whitelist) == 0 {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return errors.New("models: unable to update sr_passivated_variation_versions, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `sr_passivated_variation_versions` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, srPassivatedVariationVersionPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(srPassivatedVariationVersionType, srPassivatedVariationVersionMapping, append(wl, srPassivatedVariationVersionPrimaryKeyColumns...))
		if err != nil {
			return err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	_, err = exec.Exec(cache.query, values...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update sr_passivated_variation_versions row")
	}

	if !cached {
		srPassivatedVariationVersionUpdateCacheMut.Lock()
		srPassivatedVariationVersionUpdateCache[key] = cache
		srPassivatedVariationVersionUpdateCacheMut.Unlock()
	}

	return o.doAfterUpdateHooks(exec)
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q srPassivatedVariationVersionQuery) UpdateAllP(cols M) {
	if err := q.UpdateAll(cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values.
func (q srPassivatedVariationVersionQuery) UpdateAll(cols M) error {
	queries.SetUpdate(q.Query, cols)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "models: unable to update all for sr_passivated_variation_versions")
	}

	return nil
}

// UpdateAllG updates all rows with the specified column values.
func (o SRPassivatedVariationVersionSlice) UpdateAllG(cols M) error {
	return o.UpdateAll(boil.GetDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o SRPassivatedVariationVersionSlice) UpdateAllGP(cols M) {
	if err := o.UpdateAll(boil.GetDB(), cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o SRPassivatedVariationVersionSlice) UpdateAllP(exec boil.Executor, cols M) {
	if err := o.UpdateAll(exec, cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o SRPassivatedVariationVersionSlice) UpdateAll(exec boil.Executor, cols M) error {
	ln := int64(len(o))
	if ln == 0 {
		return nil
	}

	if len(cols) == 0 {
		return errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), srPassivatedVariationVersionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `sr_passivated_variation_versions` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, srPassivatedVariationVersionPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update all in srPassivatedVariationVersion slice")
	}

	return nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *SRPassivatedVariationVersion) UpsertG(updateColumns []string, whitelist ...string) error {
	return o.Upsert(boil.GetDB(), updateColumns, whitelist...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *SRPassivatedVariationVersion) UpsertGP(updateColumns []string, whitelist ...string) {
	if err := o.Upsert(boil.GetDB(), updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *SRPassivatedVariationVersion) UpsertP(exec boil.Executor, updateColumns []string, whitelist ...string) {
	if err := o.Upsert(exec, updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
func (o *SRPassivatedVariationVersion) Upsert(exec boil.Executor, updateColumns []string, whitelist ...string) error {
	if o == nil {
		return errors.New("models: no sr_passivated_variation_versions provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(srPassivatedVariationVersionColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs postgres problems
	buf := strmangle.GetBuffer()
	for _, c := range updateColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range whitelist {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	srPassivatedVariationVersionUpsertCacheMut.RLock()
	cache, cached := srPassivatedVariationVersionUpsertCache[key]
	srPassivatedVariationVersionUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := strmangle.InsertColumnSet(
			srPassivatedVariationVersionColumns,
			srPassivatedVariationVersionColumnsWithDefault,
			srPassivatedVariationVersionColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)

		update := strmangle.UpdateColumnSet(
			srPassivatedVariationVersionColumns,
			srPassivatedVariationVersionPrimaryKeyColumns,
			updateColumns,
		)
		if len(update) == 0 {
			return errors.New("models: unable to upsert sr_passivated_variation_versions, could not build update column list")
		}

		cache.query = queries.BuildUpsertQueryMySQL(dialect, "sr_passivated_variation_versions", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `sr_passivated_variation_versions` WHERE `id`=?",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
		)

		cache.valueMapping, err = queries.BindMapping(srPassivatedVariationVersionType, srPassivatedVariationVersionMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(srPassivatedVariationVersionType, srPassivatedVariationVersionMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.Exec(cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for sr_passivated_variation_versions")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == srPassivatedVariationVersionMapping["ID"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, identifierCols...)
	}

	err = exec.QueryRow(cache.retQuery, identifierCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for sr_passivated_variation_versions")
	}

CacheNoHooks:
	if !cached {
		srPassivatedVariationVersionUpsertCacheMut.Lock()
		srPassivatedVariationVersionUpsertCache[key] = cache
		srPassivatedVariationVersionUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(exec)
}

// DeleteP deletes a single SRPassivatedVariationVersion record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *SRPassivatedVariationVersion) DeleteP(exec boil.Executor) {
	if err := o.Delete(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteG deletes a single SRPassivatedVariationVersion record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *SRPassivatedVariationVersion) DeleteG() error {
	if o == nil {
		return errors.New("models: no SRPassivatedVariationVersion provided for deletion")
	}

	return o.Delete(boil.GetDB())
}

// DeleteGP deletes a single SRPassivatedVariationVersion record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *SRPassivatedVariationVersion) DeleteGP() {
	if err := o.DeleteG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Delete deletes a single SRPassivatedVariationVersion record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *SRPassivatedVariationVersion) Delete(exec boil.Executor) error {
	if o == nil {
		return errors.New("models: no SRPassivatedVariationVersion provided for delete")
	}

	if err := o.doBeforeDeleteHooks(exec); err != nil {
		return err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), srPassivatedVariationVersionPrimaryKeyMapping)
	sql := "DELETE FROM `sr_passivated_variation_versions` WHERE `id`=?"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete from sr_passivated_variation_versions")
	}

	if err := o.doAfterDeleteHooks(exec); err != nil {
		return err
	}

	return nil
}

// DeleteAllP deletes all rows, and panics on error.
func (q srPassivatedVariationVersionQuery) DeleteAllP() {
	if err := q.DeleteAll(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all matching rows.
func (q srPassivatedVariationVersionQuery) DeleteAll() error {
	if q.Query == nil {
		return errors.New("models: no srPassivatedVariationVersionQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from sr_passivated_variation_versions")
	}

	return nil
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o SRPassivatedVariationVersionSlice) DeleteAllGP() {
	if err := o.DeleteAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAllG deletes all rows in the slice.
func (o SRPassivatedVariationVersionSlice) DeleteAllG() error {
	if o == nil {
		return errors.New("models: no SRPassivatedVariationVersion slice provided for delete all")
	}
	return o.DeleteAll(boil.GetDB())
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o SRPassivatedVariationVersionSlice) DeleteAllP(exec boil.Executor) {
	if err := o.DeleteAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o SRPassivatedVariationVersionSlice) DeleteAll(exec boil.Executor) error {
	if o == nil {
		return errors.New("models: no SRPassivatedVariationVersion slice provided for delete all")
	}

	if len(o) == 0 {
		return nil
	}

	if len(srPassivatedVariationVersionBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(exec); err != nil {
				return err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), srPassivatedVariationVersionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `sr_passivated_variation_versions` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, srPassivatedVariationVersionPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from srPassivatedVariationVersion slice")
	}

	if len(srPassivatedVariationVersionAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(exec); err != nil {
				return err
			}
		}
	}

	return nil
}

// ReloadGP refetches the object from the database and panics on error.
func (o *SRPassivatedVariationVersion) ReloadGP() {
	if err := o.ReloadG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *SRPassivatedVariationVersion) ReloadP(exec boil.Executor) {
	if err := o.Reload(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadG refetches the object from the database using the primary keys.
func (o *SRPassivatedVariationVersion) ReloadG() error {
	if o == nil {
		return errors.New("models: no SRPassivatedVariationVersion provided for reload")
	}

	return o.Reload(boil.GetDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *SRPassivatedVariationVersion) Reload(exec boil.Executor) error {
	ret, err := FindSRPassivatedVariationVersion(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *SRPassivatedVariationVersionSlice) ReloadAllGP() {
	if err := o.ReloadAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *SRPassivatedVariationVersionSlice) ReloadAllP(exec boil.Executor) {
	if err := o.ReloadAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *SRPassivatedVariationVersionSlice) ReloadAllG() error {
	if o == nil {
		return errors.New("models: empty SRPassivatedVariationVersionSlice provided for reload all")
	}

	return o.ReloadAll(boil.GetDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *SRPassivatedVariationVersionSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	srPassivatedVariationVersions := SRPassivatedVariationVersionSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), srPassivatedVariationVersionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `sr_passivated_variation_versions`.* FROM `sr_passivated_variation_versions` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, srPassivatedVariationVersionPrimaryKeyColumns, len(*o))

	q := queries.Raw(exec, sql, args...)

	err := q.Bind(&srPassivatedVariationVersions)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in SRPassivatedVariationVersionSlice")
	}

	*o = srPassivatedVariationVersions

	return nil
}

// SRPassivatedVariationVersionExists checks if the SRPassivatedVariationVersion row exists.
func SRPassivatedVariationVersionExists(exec boil.Executor, id uint64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `sr_passivated_variation_versions` where `id`=? limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, id)
	}

	row := exec.QueryRow(sql, id)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if sr_passivated_variation_versions exists")
	}

	return exists, nil
}

// SRPassivatedVariationVersionExistsG checks if the SRPassivatedVariationVersion row exists.
func SRPassivatedVariationVersionExistsG(id uint64) (bool, error) {
	return SRPassivatedVariationVersionExists(boil.GetDB(), id)
}

// SRPassivatedVariationVersionExistsGP checks if the SRPassivatedVariationVersion row exists. Panics on error.
func SRPassivatedVariationVersionExistsGP(id uint64) bool {
	e, err := SRPassivatedVariationVersionExists(boil.GetDB(), id)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// SRPassivatedVariationVersionExistsP checks if the SRPassivatedVariationVersion row exists. Panics on error.
func SRPassivatedVariationVersionExistsP(exec boil.Executor, id uint64) bool {
	e, err := SRPassivatedVariationVersionExists(exec, id)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}
