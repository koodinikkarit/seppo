// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"database/sql"
	"fmt"
	"reflect"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// SRNewVariationVersion is an object representing the database table.
type SRNewVariationVersion struct {
	ID                 uint64 `boil:"id" json:"id" toml:"id" yaml:"id"`
	SRID               uint64 `boil:"sr_id" json:"sr_id" toml:"sr_id" yaml:"sr_id"`
	VariationVersionID uint64 `boil:"variation_version_id" json:"variation_version_id" toml:"variation_version_id" yaml:"variation_version_id"`

	R *srNewVariationVersionR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L srNewVariationVersionL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var SRNewVariationVersionColumns = struct {
	ID                 string
	SRID               string
	VariationVersionID string
}{
	ID:                 "id",
	SRID:               "sr_id",
	VariationVersionID: "variation_version_id",
}

// srNewVariationVersionR is where relationships are stored.
type srNewVariationVersionR struct {
	SR               *SynchronizationRaport
	VariationVersion *VariationVersion
}

// srNewVariationVersionL is where Load methods for each relationship are stored.
type srNewVariationVersionL struct{}

var (
	srNewVariationVersionColumns               = []string{"id", "sr_id", "variation_version_id"}
	srNewVariationVersionColumnsWithoutDefault = []string{"sr_id", "variation_version_id"}
	srNewVariationVersionColumnsWithDefault    = []string{"id"}
	srNewVariationVersionPrimaryKeyColumns     = []string{"id"}
)

type (
	// SRNewVariationVersionSlice is an alias for a slice of pointers to SRNewVariationVersion.
	// This should generally be used opposed to []SRNewVariationVersion.
	SRNewVariationVersionSlice []*SRNewVariationVersion
	// SRNewVariationVersionHook is the signature for custom SRNewVariationVersion hook methods
	SRNewVariationVersionHook func(boil.Executor, *SRNewVariationVersion) error

	srNewVariationVersionQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	srNewVariationVersionType                 = reflect.TypeOf(&SRNewVariationVersion{})
	srNewVariationVersionMapping              = queries.MakeStructMapping(srNewVariationVersionType)
	srNewVariationVersionPrimaryKeyMapping, _ = queries.BindMapping(srNewVariationVersionType, srNewVariationVersionMapping, srNewVariationVersionPrimaryKeyColumns)
	srNewVariationVersionInsertCacheMut       sync.RWMutex
	srNewVariationVersionInsertCache          = make(map[string]insertCache)
	srNewVariationVersionUpdateCacheMut       sync.RWMutex
	srNewVariationVersionUpdateCache          = make(map[string]updateCache)
	srNewVariationVersionUpsertCacheMut       sync.RWMutex
	srNewVariationVersionUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force bytes in case of primary key column that uses []byte (for relationship compares)
	_ = bytes.MinRead
)
var srNewVariationVersionBeforeInsertHooks []SRNewVariationVersionHook
var srNewVariationVersionBeforeUpdateHooks []SRNewVariationVersionHook
var srNewVariationVersionBeforeDeleteHooks []SRNewVariationVersionHook
var srNewVariationVersionBeforeUpsertHooks []SRNewVariationVersionHook

var srNewVariationVersionAfterInsertHooks []SRNewVariationVersionHook
var srNewVariationVersionAfterSelectHooks []SRNewVariationVersionHook
var srNewVariationVersionAfterUpdateHooks []SRNewVariationVersionHook
var srNewVariationVersionAfterDeleteHooks []SRNewVariationVersionHook
var srNewVariationVersionAfterUpsertHooks []SRNewVariationVersionHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *SRNewVariationVersion) doBeforeInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range srNewVariationVersionBeforeInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *SRNewVariationVersion) doBeforeUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range srNewVariationVersionBeforeUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *SRNewVariationVersion) doBeforeDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range srNewVariationVersionBeforeDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *SRNewVariationVersion) doBeforeUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range srNewVariationVersionBeforeUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *SRNewVariationVersion) doAfterInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range srNewVariationVersionAfterInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *SRNewVariationVersion) doAfterSelectHooks(exec boil.Executor) (err error) {
	for _, hook := range srNewVariationVersionAfterSelectHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *SRNewVariationVersion) doAfterUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range srNewVariationVersionAfterUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *SRNewVariationVersion) doAfterDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range srNewVariationVersionAfterDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *SRNewVariationVersion) doAfterUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range srNewVariationVersionAfterUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddSRNewVariationVersionHook registers your hook function for all future operations.
func AddSRNewVariationVersionHook(hookPoint boil.HookPoint, srNewVariationVersionHook SRNewVariationVersionHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		srNewVariationVersionBeforeInsertHooks = append(srNewVariationVersionBeforeInsertHooks, srNewVariationVersionHook)
	case boil.BeforeUpdateHook:
		srNewVariationVersionBeforeUpdateHooks = append(srNewVariationVersionBeforeUpdateHooks, srNewVariationVersionHook)
	case boil.BeforeDeleteHook:
		srNewVariationVersionBeforeDeleteHooks = append(srNewVariationVersionBeforeDeleteHooks, srNewVariationVersionHook)
	case boil.BeforeUpsertHook:
		srNewVariationVersionBeforeUpsertHooks = append(srNewVariationVersionBeforeUpsertHooks, srNewVariationVersionHook)
	case boil.AfterInsertHook:
		srNewVariationVersionAfterInsertHooks = append(srNewVariationVersionAfterInsertHooks, srNewVariationVersionHook)
	case boil.AfterSelectHook:
		srNewVariationVersionAfterSelectHooks = append(srNewVariationVersionAfterSelectHooks, srNewVariationVersionHook)
	case boil.AfterUpdateHook:
		srNewVariationVersionAfterUpdateHooks = append(srNewVariationVersionAfterUpdateHooks, srNewVariationVersionHook)
	case boil.AfterDeleteHook:
		srNewVariationVersionAfterDeleteHooks = append(srNewVariationVersionAfterDeleteHooks, srNewVariationVersionHook)
	case boil.AfterUpsertHook:
		srNewVariationVersionAfterUpsertHooks = append(srNewVariationVersionAfterUpsertHooks, srNewVariationVersionHook)
	}
}

// OneP returns a single srNewVariationVersion record from the query, and panics on error.
func (q srNewVariationVersionQuery) OneP() *SRNewVariationVersion {
	o, err := q.One()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single srNewVariationVersion record from the query.
func (q srNewVariationVersionQuery) One() (*SRNewVariationVersion, error) {
	o := &SRNewVariationVersion{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for sr_new_variation_versions")
	}

	if err := o.doAfterSelectHooks(queries.GetExecutor(q.Query)); err != nil {
		return o, err
	}

	return o, nil
}

// AllP returns all SRNewVariationVersion records from the query, and panics on error.
func (q srNewVariationVersionQuery) AllP() SRNewVariationVersionSlice {
	o, err := q.All()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all SRNewVariationVersion records from the query.
func (q srNewVariationVersionQuery) All() (SRNewVariationVersionSlice, error) {
	var o []*SRNewVariationVersion

	err := q.Bind(&o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to SRNewVariationVersion slice")
	}

	if len(srNewVariationVersionAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(queries.GetExecutor(q.Query)); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountP returns the count of all SRNewVariationVersion records in the query, and panics on error.
func (q srNewVariationVersionQuery) CountP() int64 {
	c, err := q.Count()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all SRNewVariationVersion records in the query.
func (q srNewVariationVersionQuery) Count() (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count sr_new_variation_versions rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table, and panics on error.
func (q srNewVariationVersionQuery) ExistsP() bool {
	e, err := q.Exists()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q srNewVariationVersionQuery) Exists() (bool, error) {
	var count int64

	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if sr_new_variation_versions exists")
	}

	return count > 0, nil
}

// SRG pointed to by the foreign key.
func (o *SRNewVariationVersion) SRG(mods ...qm.QueryMod) synchronizationRaportQuery {
	return o.SR(boil.GetDB(), mods...)
}

// SR pointed to by the foreign key.
func (o *SRNewVariationVersion) SR(exec boil.Executor, mods ...qm.QueryMod) synchronizationRaportQuery {
	queryMods := []qm.QueryMod{
		qm.Where("id=?", o.SRID),
	}

	queryMods = append(queryMods, mods...)

	query := SynchronizationRaports(exec, queryMods...)
	queries.SetFrom(query.Query, "`synchronization_raports`")

	return query
}

// VariationVersionG pointed to by the foreign key.
func (o *SRNewVariationVersion) VariationVersionG(mods ...qm.QueryMod) variationVersionQuery {
	return o.VariationVersion(boil.GetDB(), mods...)
}

// VariationVersion pointed to by the foreign key.
func (o *SRNewVariationVersion) VariationVersion(exec boil.Executor, mods ...qm.QueryMod) variationVersionQuery {
	queryMods := []qm.QueryMod{
		qm.Where("id=?", o.VariationVersionID),
	}

	queryMods = append(queryMods, mods...)

	query := VariationVersions(exec, queryMods...)
	queries.SetFrom(query.Query, "`variation_versions`")

	return query
} // LoadSR allows an eager lookup of values, cached into the
// loaded structs of the objects.
func (srNewVariationVersionL) LoadSR(e boil.Executor, singular bool, maybeSRNewVariationVersion interface{}) error {
	var slice []*SRNewVariationVersion
	var object *SRNewVariationVersion

	count := 1
	if singular {
		object = maybeSRNewVariationVersion.(*SRNewVariationVersion)
	} else {
		slice = *maybeSRNewVariationVersion.(*[]*SRNewVariationVersion)
		count = len(slice)
	}

	args := make([]interface{}, count)
	if singular {
		if object.R == nil {
			object.R = &srNewVariationVersionR{}
		}
		args[0] = object.SRID
	} else {
		for i, obj := range slice {
			if obj.R == nil {
				obj.R = &srNewVariationVersionR{}
			}
			args[i] = obj.SRID
		}
	}

	query := fmt.Sprintf(
		"select * from `synchronization_raports` where `id` in (%s)",
		strmangle.Placeholders(dialect.IndexPlaceholders, count, 1, 1),
	)

	if boil.DebugMode {
		fmt.Fprintf(boil.DebugWriter, "%s\n%v\n", query, args)
	}

	results, err := e.Query(query, args...)
	if err != nil {
		return errors.Wrap(err, "failed to eager load SynchronizationRaport")
	}
	defer results.Close()

	var resultSlice []*SynchronizationRaport
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice SynchronizationRaport")
	}

	if len(srNewVariationVersionAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		object.R.SR = resultSlice[0]
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.SRID == foreign.ID {
				local.R.SR = foreign
				break
			}
		}
	}

	return nil
}

// LoadVariationVersion allows an eager lookup of values, cached into the
// loaded structs of the objects.
func (srNewVariationVersionL) LoadVariationVersion(e boil.Executor, singular bool, maybeSRNewVariationVersion interface{}) error {
	var slice []*SRNewVariationVersion
	var object *SRNewVariationVersion

	count := 1
	if singular {
		object = maybeSRNewVariationVersion.(*SRNewVariationVersion)
	} else {
		slice = *maybeSRNewVariationVersion.(*[]*SRNewVariationVersion)
		count = len(slice)
	}

	args := make([]interface{}, count)
	if singular {
		if object.R == nil {
			object.R = &srNewVariationVersionR{}
		}
		args[0] = object.VariationVersionID
	} else {
		for i, obj := range slice {
			if obj.R == nil {
				obj.R = &srNewVariationVersionR{}
			}
			args[i] = obj.VariationVersionID
		}
	}

	query := fmt.Sprintf(
		"select * from `variation_versions` where `id` in (%s)",
		strmangle.Placeholders(dialect.IndexPlaceholders, count, 1, 1),
	)

	if boil.DebugMode {
		fmt.Fprintf(boil.DebugWriter, "%s\n%v\n", query, args)
	}

	results, err := e.Query(query, args...)
	if err != nil {
		return errors.Wrap(err, "failed to eager load VariationVersion")
	}
	defer results.Close()

	var resultSlice []*VariationVersion
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice VariationVersion")
	}

	if len(srNewVariationVersionAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		object.R.VariationVersion = resultSlice[0]
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.VariationVersionID == foreign.ID {
				local.R.VariationVersion = foreign
				break
			}
		}
	}

	return nil
}

// SetSRG of the sr_new_variation_version to the related item.
// Sets o.R.SR to related.
// Adds o to related.R.SRSRNewVariationVersions.
// Uses the global database handle.
func (o *SRNewVariationVersion) SetSRG(insert bool, related *SynchronizationRaport) error {
	return o.SetSR(boil.GetDB(), insert, related)
}

// SetSRP of the sr_new_variation_version to the related item.
// Sets o.R.SR to related.
// Adds o to related.R.SRSRNewVariationVersions.
// Panics on error.
func (o *SRNewVariationVersion) SetSRP(exec boil.Executor, insert bool, related *SynchronizationRaport) {
	if err := o.SetSR(exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetSRGP of the sr_new_variation_version to the related item.
// Sets o.R.SR to related.
// Adds o to related.R.SRSRNewVariationVersions.
// Uses the global database handle and panics on error.
func (o *SRNewVariationVersion) SetSRGP(insert bool, related *SynchronizationRaport) {
	if err := o.SetSR(boil.GetDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetSR of the sr_new_variation_version to the related item.
// Sets o.R.SR to related.
// Adds o to related.R.SRSRNewVariationVersions.
func (o *SRNewVariationVersion) SetSR(exec boil.Executor, insert bool, related *SynchronizationRaport) error {
	var err error
	if insert {
		if err = related.Insert(exec); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `sr_new_variation_versions` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"sr_id"}),
		strmangle.WhereClause("`", "`", 0, srNewVariationVersionPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.SRID = related.ID

	if o.R == nil {
		o.R = &srNewVariationVersionR{
			SR: related,
		}
	} else {
		o.R.SR = related
	}

	if related.R == nil {
		related.R = &synchronizationRaportR{
			SRSRNewVariationVersions: SRNewVariationVersionSlice{o},
		}
	} else {
		related.R.SRSRNewVariationVersions = append(related.R.SRSRNewVariationVersions, o)
	}

	return nil
}

// SetVariationVersionG of the sr_new_variation_version to the related item.
// Sets o.R.VariationVersion to related.
// Adds o to related.R.SRNewVariationVersions.
// Uses the global database handle.
func (o *SRNewVariationVersion) SetVariationVersionG(insert bool, related *VariationVersion) error {
	return o.SetVariationVersion(boil.GetDB(), insert, related)
}

// SetVariationVersionP of the sr_new_variation_version to the related item.
// Sets o.R.VariationVersion to related.
// Adds o to related.R.SRNewVariationVersions.
// Panics on error.
func (o *SRNewVariationVersion) SetVariationVersionP(exec boil.Executor, insert bool, related *VariationVersion) {
	if err := o.SetVariationVersion(exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetVariationVersionGP of the sr_new_variation_version to the related item.
// Sets o.R.VariationVersion to related.
// Adds o to related.R.SRNewVariationVersions.
// Uses the global database handle and panics on error.
func (o *SRNewVariationVersion) SetVariationVersionGP(insert bool, related *VariationVersion) {
	if err := o.SetVariationVersion(boil.GetDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetVariationVersion of the sr_new_variation_version to the related item.
// Sets o.R.VariationVersion to related.
// Adds o to related.R.SRNewVariationVersions.
func (o *SRNewVariationVersion) SetVariationVersion(exec boil.Executor, insert bool, related *VariationVersion) error {
	var err error
	if insert {
		if err = related.Insert(exec); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `sr_new_variation_versions` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"variation_version_id"}),
		strmangle.WhereClause("`", "`", 0, srNewVariationVersionPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.VariationVersionID = related.ID

	if o.R == nil {
		o.R = &srNewVariationVersionR{
			VariationVersion: related,
		}
	} else {
		o.R.VariationVersion = related
	}

	if related.R == nil {
		related.R = &variationVersionR{
			SRNewVariationVersions: SRNewVariationVersionSlice{o},
		}
	} else {
		related.R.SRNewVariationVersions = append(related.R.SRNewVariationVersions, o)
	}

	return nil
}

// SRNewVariationVersionsG retrieves all records.
func SRNewVariationVersionsG(mods ...qm.QueryMod) srNewVariationVersionQuery {
	return SRNewVariationVersions(boil.GetDB(), mods...)
}

// SRNewVariationVersions retrieves all the records using an executor.
func SRNewVariationVersions(exec boil.Executor, mods ...qm.QueryMod) srNewVariationVersionQuery {
	mods = append(mods, qm.From("`sr_new_variation_versions`"))
	return srNewVariationVersionQuery{NewQuery(exec, mods...)}
}

// FindSRNewVariationVersionG retrieves a single record by ID.
func FindSRNewVariationVersionG(id uint64, selectCols ...string) (*SRNewVariationVersion, error) {
	return FindSRNewVariationVersion(boil.GetDB(), id, selectCols...)
}

// FindSRNewVariationVersionGP retrieves a single record by ID, and panics on error.
func FindSRNewVariationVersionGP(id uint64, selectCols ...string) *SRNewVariationVersion {
	retobj, err := FindSRNewVariationVersion(boil.GetDB(), id, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindSRNewVariationVersion retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindSRNewVariationVersion(exec boil.Executor, id uint64, selectCols ...string) (*SRNewVariationVersion, error) {
	srNewVariationVersionObj := &SRNewVariationVersion{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `sr_new_variation_versions` where `id`=?", sel,
	)

	q := queries.Raw(exec, query, id)

	err := q.Bind(srNewVariationVersionObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from sr_new_variation_versions")
	}

	return srNewVariationVersionObj, nil
}

// FindSRNewVariationVersionP retrieves a single record by ID with an executor, and panics on error.
func FindSRNewVariationVersionP(exec boil.Executor, id uint64, selectCols ...string) *SRNewVariationVersion {
	retobj, err := FindSRNewVariationVersion(exec, id, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *SRNewVariationVersion) InsertG(whitelist ...string) error {
	return o.Insert(boil.GetDB(), whitelist...)
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *SRNewVariationVersion) InsertGP(whitelist ...string) {
	if err := o.Insert(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *SRNewVariationVersion) InsertP(exec boil.Executor, whitelist ...string) {
	if err := o.Insert(exec, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// Whitelist behavior: If a whitelist is provided, only those columns supplied are inserted
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns without a default value are included (i.e. name, age)
// - All columns with a default, but non-zero are included (i.e. health = 75)
func (o *SRNewVariationVersion) Insert(exec boil.Executor, whitelist ...string) error {
	if o == nil {
		return errors.New("models: no sr_new_variation_versions provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(srNewVariationVersionColumnsWithDefault, o)

	key := makeCacheKey(whitelist, nzDefaults)
	srNewVariationVersionInsertCacheMut.RLock()
	cache, cached := srNewVariationVersionInsertCache[key]
	srNewVariationVersionInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := strmangle.InsertColumnSet(
			srNewVariationVersionColumns,
			srNewVariationVersionColumnsWithDefault,
			srNewVariationVersionColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)

		cache.valueMapping, err = queries.BindMapping(srNewVariationVersionType, srNewVariationVersionMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(srNewVariationVersionType, srNewVariationVersionMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `sr_new_variation_versions` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.IndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `sr_new_variation_versions` () VALUES ()"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `sr_new_variation_versions` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, srNewVariationVersionPrimaryKeyColumns))
		}

		if len(wl) != 0 {
			cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.Exec(cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into sr_new_variation_versions")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == srNewVariationVersionMapping["ID"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, identifierCols...)
	}

	err = exec.QueryRow(cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for sr_new_variation_versions")
	}

CacheNoHooks:
	if !cached {
		srNewVariationVersionInsertCacheMut.Lock()
		srNewVariationVersionInsertCache[key] = cache
		srNewVariationVersionInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(exec)
}

// UpdateG a single SRNewVariationVersion record. See Update for
// whitelist behavior description.
func (o *SRNewVariationVersion) UpdateG(whitelist ...string) error {
	return o.Update(boil.GetDB(), whitelist...)
}

// UpdateGP a single SRNewVariationVersion record.
// UpdateGP takes a whitelist of column names that should be updated.
// Panics on error. See Update for whitelist behavior description.
func (o *SRNewVariationVersion) UpdateGP(whitelist ...string) {
	if err := o.Update(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateP uses an executor to update the SRNewVariationVersion, and panics on error.
// See Update for whitelist behavior description.
func (o *SRNewVariationVersion) UpdateP(exec boil.Executor, whitelist ...string) {
	err := o.Update(exec, whitelist...)
	if err != nil {
		panic(boil.WrapErr(err))
	}
}

// Update uses an executor to update the SRNewVariationVersion.
// Whitelist behavior: If a whitelist is provided, only the columns given are updated.
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns are inferred to start with
// - All primary keys are subtracted from this set
// Update does not automatically update the record in case of default values. Use .Reload()
// to refresh the records.
func (o *SRNewVariationVersion) Update(exec boil.Executor, whitelist ...string) error {
	var err error
	if err = o.doBeforeUpdateHooks(exec); err != nil {
		return err
	}
	key := makeCacheKey(whitelist, nil)
	srNewVariationVersionUpdateCacheMut.RLock()
	cache, cached := srNewVariationVersionUpdateCache[key]
	srNewVariationVersionUpdateCacheMut.RUnlock()

	if !cached {
		wl := strmangle.UpdateColumnSet(
			srNewVariationVersionColumns,
			srNewVariationVersionPrimaryKeyColumns,
			whitelist,
		)

		if len(whitelist) == 0 {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return errors.New("models: unable to update sr_new_variation_versions, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `sr_new_variation_versions` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, srNewVariationVersionPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(srNewVariationVersionType, srNewVariationVersionMapping, append(wl, srNewVariationVersionPrimaryKeyColumns...))
		if err != nil {
			return err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	_, err = exec.Exec(cache.query, values...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update sr_new_variation_versions row")
	}

	if !cached {
		srNewVariationVersionUpdateCacheMut.Lock()
		srNewVariationVersionUpdateCache[key] = cache
		srNewVariationVersionUpdateCacheMut.Unlock()
	}

	return o.doAfterUpdateHooks(exec)
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q srNewVariationVersionQuery) UpdateAllP(cols M) {
	if err := q.UpdateAll(cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values.
func (q srNewVariationVersionQuery) UpdateAll(cols M) error {
	queries.SetUpdate(q.Query, cols)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "models: unable to update all for sr_new_variation_versions")
	}

	return nil
}

// UpdateAllG updates all rows with the specified column values.
func (o SRNewVariationVersionSlice) UpdateAllG(cols M) error {
	return o.UpdateAll(boil.GetDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o SRNewVariationVersionSlice) UpdateAllGP(cols M) {
	if err := o.UpdateAll(boil.GetDB(), cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o SRNewVariationVersionSlice) UpdateAllP(exec boil.Executor, cols M) {
	if err := o.UpdateAll(exec, cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o SRNewVariationVersionSlice) UpdateAll(exec boil.Executor, cols M) error {
	ln := int64(len(o))
	if ln == 0 {
		return nil
	}

	if len(cols) == 0 {
		return errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), srNewVariationVersionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `sr_new_variation_versions` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, srNewVariationVersionPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update all in srNewVariationVersion slice")
	}

	return nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *SRNewVariationVersion) UpsertG(updateColumns []string, whitelist ...string) error {
	return o.Upsert(boil.GetDB(), updateColumns, whitelist...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *SRNewVariationVersion) UpsertGP(updateColumns []string, whitelist ...string) {
	if err := o.Upsert(boil.GetDB(), updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *SRNewVariationVersion) UpsertP(exec boil.Executor, updateColumns []string, whitelist ...string) {
	if err := o.Upsert(exec, updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
func (o *SRNewVariationVersion) Upsert(exec boil.Executor, updateColumns []string, whitelist ...string) error {
	if o == nil {
		return errors.New("models: no sr_new_variation_versions provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(srNewVariationVersionColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs postgres problems
	buf := strmangle.GetBuffer()
	for _, c := range updateColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range whitelist {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	srNewVariationVersionUpsertCacheMut.RLock()
	cache, cached := srNewVariationVersionUpsertCache[key]
	srNewVariationVersionUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := strmangle.InsertColumnSet(
			srNewVariationVersionColumns,
			srNewVariationVersionColumnsWithDefault,
			srNewVariationVersionColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)

		update := strmangle.UpdateColumnSet(
			srNewVariationVersionColumns,
			srNewVariationVersionPrimaryKeyColumns,
			updateColumns,
		)
		if len(update) == 0 {
			return errors.New("models: unable to upsert sr_new_variation_versions, could not build update column list")
		}

		cache.query = queries.BuildUpsertQueryMySQL(dialect, "sr_new_variation_versions", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `sr_new_variation_versions` WHERE `id`=?",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
		)

		cache.valueMapping, err = queries.BindMapping(srNewVariationVersionType, srNewVariationVersionMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(srNewVariationVersionType, srNewVariationVersionMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.Exec(cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for sr_new_variation_versions")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == srNewVariationVersionMapping["ID"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, identifierCols...)
	}

	err = exec.QueryRow(cache.retQuery, identifierCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for sr_new_variation_versions")
	}

CacheNoHooks:
	if !cached {
		srNewVariationVersionUpsertCacheMut.Lock()
		srNewVariationVersionUpsertCache[key] = cache
		srNewVariationVersionUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(exec)
}

// DeleteP deletes a single SRNewVariationVersion record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *SRNewVariationVersion) DeleteP(exec boil.Executor) {
	if err := o.Delete(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteG deletes a single SRNewVariationVersion record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *SRNewVariationVersion) DeleteG() error {
	if o == nil {
		return errors.New("models: no SRNewVariationVersion provided for deletion")
	}

	return o.Delete(boil.GetDB())
}

// DeleteGP deletes a single SRNewVariationVersion record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *SRNewVariationVersion) DeleteGP() {
	if err := o.DeleteG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Delete deletes a single SRNewVariationVersion record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *SRNewVariationVersion) Delete(exec boil.Executor) error {
	if o == nil {
		return errors.New("models: no SRNewVariationVersion provided for delete")
	}

	if err := o.doBeforeDeleteHooks(exec); err != nil {
		return err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), srNewVariationVersionPrimaryKeyMapping)
	sql := "DELETE FROM `sr_new_variation_versions` WHERE `id`=?"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete from sr_new_variation_versions")
	}

	if err := o.doAfterDeleteHooks(exec); err != nil {
		return err
	}

	return nil
}

// DeleteAllP deletes all rows, and panics on error.
func (q srNewVariationVersionQuery) DeleteAllP() {
	if err := q.DeleteAll(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all matching rows.
func (q srNewVariationVersionQuery) DeleteAll() error {
	if q.Query == nil {
		return errors.New("models: no srNewVariationVersionQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from sr_new_variation_versions")
	}

	return nil
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o SRNewVariationVersionSlice) DeleteAllGP() {
	if err := o.DeleteAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAllG deletes all rows in the slice.
func (o SRNewVariationVersionSlice) DeleteAllG() error {
	if o == nil {
		return errors.New("models: no SRNewVariationVersion slice provided for delete all")
	}
	return o.DeleteAll(boil.GetDB())
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o SRNewVariationVersionSlice) DeleteAllP(exec boil.Executor) {
	if err := o.DeleteAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o SRNewVariationVersionSlice) DeleteAll(exec boil.Executor) error {
	if o == nil {
		return errors.New("models: no SRNewVariationVersion slice provided for delete all")
	}

	if len(o) == 0 {
		return nil
	}

	if len(srNewVariationVersionBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(exec); err != nil {
				return err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), srNewVariationVersionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `sr_new_variation_versions` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, srNewVariationVersionPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from srNewVariationVersion slice")
	}

	if len(srNewVariationVersionAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(exec); err != nil {
				return err
			}
		}
	}

	return nil
}

// ReloadGP refetches the object from the database and panics on error.
func (o *SRNewVariationVersion) ReloadGP() {
	if err := o.ReloadG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *SRNewVariationVersion) ReloadP(exec boil.Executor) {
	if err := o.Reload(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadG refetches the object from the database using the primary keys.
func (o *SRNewVariationVersion) ReloadG() error {
	if o == nil {
		return errors.New("models: no SRNewVariationVersion provided for reload")
	}

	return o.Reload(boil.GetDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *SRNewVariationVersion) Reload(exec boil.Executor) error {
	ret, err := FindSRNewVariationVersion(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *SRNewVariationVersionSlice) ReloadAllGP() {
	if err := o.ReloadAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *SRNewVariationVersionSlice) ReloadAllP(exec boil.Executor) {
	if err := o.ReloadAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *SRNewVariationVersionSlice) ReloadAllG() error {
	if o == nil {
		return errors.New("models: empty SRNewVariationVersionSlice provided for reload all")
	}

	return o.ReloadAll(boil.GetDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *SRNewVariationVersionSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	srNewVariationVersions := SRNewVariationVersionSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), srNewVariationVersionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `sr_new_variation_versions`.* FROM `sr_new_variation_versions` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, srNewVariationVersionPrimaryKeyColumns, len(*o))

	q := queries.Raw(exec, sql, args...)

	err := q.Bind(&srNewVariationVersions)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in SRNewVariationVersionSlice")
	}

	*o = srNewVariationVersions

	return nil
}

// SRNewVariationVersionExists checks if the SRNewVariationVersion row exists.
func SRNewVariationVersionExists(exec boil.Executor, id uint64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `sr_new_variation_versions` where `id`=? limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, id)
	}

	row := exec.QueryRow(sql, id)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if sr_new_variation_versions exists")
	}

	return exists, nil
}

// SRNewVariationVersionExistsG checks if the SRNewVariationVersion row exists.
func SRNewVariationVersionExistsG(id uint64) (bool, error) {
	return SRNewVariationVersionExists(boil.GetDB(), id)
}

// SRNewVariationVersionExistsGP checks if the SRNewVariationVersion row exists. Panics on error.
func SRNewVariationVersionExistsGP(id uint64) bool {
	e, err := SRNewVariationVersionExists(boil.GetDB(), id)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// SRNewVariationVersionExistsP checks if the SRNewVariationVersion row exists. Panics on error.
func SRNewVariationVersionExistsP(exec boil.Executor, id uint64) bool {
	e, err := SRNewVariationVersionExists(exec, id)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}
