// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"database/sql"
	"fmt"
	"reflect"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// SRNewCopyright is an object representing the database table.
type SRNewCopyright struct {
	ID          uint64 `boil:"id" json:"id" toml:"id" yaml:"id"`
	SRID        uint64 `boil:"sr_id" json:"sr_id" toml:"sr_id" yaml:"sr_id"`
	CopyrightID uint64 `boil:"copyright_id" json:"copyright_id" toml:"copyright_id" yaml:"copyright_id"`

	R *srNewCopyrightR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L srNewCopyrightL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var SRNewCopyrightColumns = struct {
	ID          string
	SRID        string
	CopyrightID string
}{
	ID:          "id",
	SRID:        "sr_id",
	CopyrightID: "copyright_id",
}

// srNewCopyrightR is where relationships are stored.
type srNewCopyrightR struct {
	SR        *SynchronizationRaport
	Copyright *Copyright
}

// srNewCopyrightL is where Load methods for each relationship are stored.
type srNewCopyrightL struct{}

var (
	srNewCopyrightColumns               = []string{"id", "sr_id", "copyright_id"}
	srNewCopyrightColumnsWithoutDefault = []string{"sr_id", "copyright_id"}
	srNewCopyrightColumnsWithDefault    = []string{"id"}
	srNewCopyrightPrimaryKeyColumns     = []string{"id"}
)

type (
	// SRNewCopyrightSlice is an alias for a slice of pointers to SRNewCopyright.
	// This should generally be used opposed to []SRNewCopyright.
	SRNewCopyrightSlice []*SRNewCopyright
	// SRNewCopyrightHook is the signature for custom SRNewCopyright hook methods
	SRNewCopyrightHook func(boil.Executor, *SRNewCopyright) error

	srNewCopyrightQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	srNewCopyrightType                 = reflect.TypeOf(&SRNewCopyright{})
	srNewCopyrightMapping              = queries.MakeStructMapping(srNewCopyrightType)
	srNewCopyrightPrimaryKeyMapping, _ = queries.BindMapping(srNewCopyrightType, srNewCopyrightMapping, srNewCopyrightPrimaryKeyColumns)
	srNewCopyrightInsertCacheMut       sync.RWMutex
	srNewCopyrightInsertCache          = make(map[string]insertCache)
	srNewCopyrightUpdateCacheMut       sync.RWMutex
	srNewCopyrightUpdateCache          = make(map[string]updateCache)
	srNewCopyrightUpsertCacheMut       sync.RWMutex
	srNewCopyrightUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force bytes in case of primary key column that uses []byte (for relationship compares)
	_ = bytes.MinRead
)
var srNewCopyrightBeforeInsertHooks []SRNewCopyrightHook
var srNewCopyrightBeforeUpdateHooks []SRNewCopyrightHook
var srNewCopyrightBeforeDeleteHooks []SRNewCopyrightHook
var srNewCopyrightBeforeUpsertHooks []SRNewCopyrightHook

var srNewCopyrightAfterInsertHooks []SRNewCopyrightHook
var srNewCopyrightAfterSelectHooks []SRNewCopyrightHook
var srNewCopyrightAfterUpdateHooks []SRNewCopyrightHook
var srNewCopyrightAfterDeleteHooks []SRNewCopyrightHook
var srNewCopyrightAfterUpsertHooks []SRNewCopyrightHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *SRNewCopyright) doBeforeInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range srNewCopyrightBeforeInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *SRNewCopyright) doBeforeUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range srNewCopyrightBeforeUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *SRNewCopyright) doBeforeDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range srNewCopyrightBeforeDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *SRNewCopyright) doBeforeUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range srNewCopyrightBeforeUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *SRNewCopyright) doAfterInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range srNewCopyrightAfterInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *SRNewCopyright) doAfterSelectHooks(exec boil.Executor) (err error) {
	for _, hook := range srNewCopyrightAfterSelectHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *SRNewCopyright) doAfterUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range srNewCopyrightAfterUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *SRNewCopyright) doAfterDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range srNewCopyrightAfterDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *SRNewCopyright) doAfterUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range srNewCopyrightAfterUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddSRNewCopyrightHook registers your hook function for all future operations.
func AddSRNewCopyrightHook(hookPoint boil.HookPoint, srNewCopyrightHook SRNewCopyrightHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		srNewCopyrightBeforeInsertHooks = append(srNewCopyrightBeforeInsertHooks, srNewCopyrightHook)
	case boil.BeforeUpdateHook:
		srNewCopyrightBeforeUpdateHooks = append(srNewCopyrightBeforeUpdateHooks, srNewCopyrightHook)
	case boil.BeforeDeleteHook:
		srNewCopyrightBeforeDeleteHooks = append(srNewCopyrightBeforeDeleteHooks, srNewCopyrightHook)
	case boil.BeforeUpsertHook:
		srNewCopyrightBeforeUpsertHooks = append(srNewCopyrightBeforeUpsertHooks, srNewCopyrightHook)
	case boil.AfterInsertHook:
		srNewCopyrightAfterInsertHooks = append(srNewCopyrightAfterInsertHooks, srNewCopyrightHook)
	case boil.AfterSelectHook:
		srNewCopyrightAfterSelectHooks = append(srNewCopyrightAfterSelectHooks, srNewCopyrightHook)
	case boil.AfterUpdateHook:
		srNewCopyrightAfterUpdateHooks = append(srNewCopyrightAfterUpdateHooks, srNewCopyrightHook)
	case boil.AfterDeleteHook:
		srNewCopyrightAfterDeleteHooks = append(srNewCopyrightAfterDeleteHooks, srNewCopyrightHook)
	case boil.AfterUpsertHook:
		srNewCopyrightAfterUpsertHooks = append(srNewCopyrightAfterUpsertHooks, srNewCopyrightHook)
	}
}

// OneP returns a single srNewCopyright record from the query, and panics on error.
func (q srNewCopyrightQuery) OneP() *SRNewCopyright {
	o, err := q.One()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single srNewCopyright record from the query.
func (q srNewCopyrightQuery) One() (*SRNewCopyright, error) {
	o := &SRNewCopyright{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for sr_new_copyrights")
	}

	if err := o.doAfterSelectHooks(queries.GetExecutor(q.Query)); err != nil {
		return o, err
	}

	return o, nil
}

// AllP returns all SRNewCopyright records from the query, and panics on error.
func (q srNewCopyrightQuery) AllP() SRNewCopyrightSlice {
	o, err := q.All()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all SRNewCopyright records from the query.
func (q srNewCopyrightQuery) All() (SRNewCopyrightSlice, error) {
	var o []*SRNewCopyright

	err := q.Bind(&o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to SRNewCopyright slice")
	}

	if len(srNewCopyrightAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(queries.GetExecutor(q.Query)); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountP returns the count of all SRNewCopyright records in the query, and panics on error.
func (q srNewCopyrightQuery) CountP() int64 {
	c, err := q.Count()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all SRNewCopyright records in the query.
func (q srNewCopyrightQuery) Count() (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count sr_new_copyrights rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table, and panics on error.
func (q srNewCopyrightQuery) ExistsP() bool {
	e, err := q.Exists()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q srNewCopyrightQuery) Exists() (bool, error) {
	var count int64

	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if sr_new_copyrights exists")
	}

	return count > 0, nil
}

// SRG pointed to by the foreign key.
func (o *SRNewCopyright) SRG(mods ...qm.QueryMod) synchronizationRaportQuery {
	return o.SR(boil.GetDB(), mods...)
}

// SR pointed to by the foreign key.
func (o *SRNewCopyright) SR(exec boil.Executor, mods ...qm.QueryMod) synchronizationRaportQuery {
	queryMods := []qm.QueryMod{
		qm.Where("id=?", o.SRID),
	}

	queryMods = append(queryMods, mods...)

	query := SynchronizationRaports(exec, queryMods...)
	queries.SetFrom(query.Query, "`synchronization_raports`")

	return query
}

// CopyrightG pointed to by the foreign key.
func (o *SRNewCopyright) CopyrightG(mods ...qm.QueryMod) copyrightQuery {
	return o.Copyright(boil.GetDB(), mods...)
}

// Copyright pointed to by the foreign key.
func (o *SRNewCopyright) Copyright(exec boil.Executor, mods ...qm.QueryMod) copyrightQuery {
	queryMods := []qm.QueryMod{
		qm.Where("id=?", o.CopyrightID),
	}

	queryMods = append(queryMods, mods...)

	query := Copyrights(exec, queryMods...)
	queries.SetFrom(query.Query, "`copyrights`")

	return query
} // LoadSR allows an eager lookup of values, cached into the
// loaded structs of the objects.
func (srNewCopyrightL) LoadSR(e boil.Executor, singular bool, maybeSRNewCopyright interface{}) error {
	var slice []*SRNewCopyright
	var object *SRNewCopyright

	count := 1
	if singular {
		object = maybeSRNewCopyright.(*SRNewCopyright)
	} else {
		slice = *maybeSRNewCopyright.(*[]*SRNewCopyright)
		count = len(slice)
	}

	args := make([]interface{}, count)
	if singular {
		if object.R == nil {
			object.R = &srNewCopyrightR{}
		}
		args[0] = object.SRID
	} else {
		for i, obj := range slice {
			if obj.R == nil {
				obj.R = &srNewCopyrightR{}
			}
			args[i] = obj.SRID
		}
	}

	query := fmt.Sprintf(
		"select * from `synchronization_raports` where `id` in (%s)",
		strmangle.Placeholders(dialect.IndexPlaceholders, count, 1, 1),
	)

	if boil.DebugMode {
		fmt.Fprintf(boil.DebugWriter, "%s\n%v\n", query, args)
	}

	results, err := e.Query(query, args...)
	if err != nil {
		return errors.Wrap(err, "failed to eager load SynchronizationRaport")
	}
	defer results.Close()

	var resultSlice []*SynchronizationRaport
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice SynchronizationRaport")
	}

	if len(srNewCopyrightAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		object.R.SR = resultSlice[0]
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.SRID == foreign.ID {
				local.R.SR = foreign
				break
			}
		}
	}

	return nil
}

// LoadCopyright allows an eager lookup of values, cached into the
// loaded structs of the objects.
func (srNewCopyrightL) LoadCopyright(e boil.Executor, singular bool, maybeSRNewCopyright interface{}) error {
	var slice []*SRNewCopyright
	var object *SRNewCopyright

	count := 1
	if singular {
		object = maybeSRNewCopyright.(*SRNewCopyright)
	} else {
		slice = *maybeSRNewCopyright.(*[]*SRNewCopyright)
		count = len(slice)
	}

	args := make([]interface{}, count)
	if singular {
		if object.R == nil {
			object.R = &srNewCopyrightR{}
		}
		args[0] = object.CopyrightID
	} else {
		for i, obj := range slice {
			if obj.R == nil {
				obj.R = &srNewCopyrightR{}
			}
			args[i] = obj.CopyrightID
		}
	}

	query := fmt.Sprintf(
		"select * from `copyrights` where `id` in (%s)",
		strmangle.Placeholders(dialect.IndexPlaceholders, count, 1, 1),
	)

	if boil.DebugMode {
		fmt.Fprintf(boil.DebugWriter, "%s\n%v\n", query, args)
	}

	results, err := e.Query(query, args...)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Copyright")
	}
	defer results.Close()

	var resultSlice []*Copyright
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Copyright")
	}

	if len(srNewCopyrightAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		object.R.Copyright = resultSlice[0]
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.CopyrightID == foreign.ID {
				local.R.Copyright = foreign
				break
			}
		}
	}

	return nil
}

// SetSRG of the sr_new_copyright to the related item.
// Sets o.R.SR to related.
// Adds o to related.R.SRSRNewCopyrights.
// Uses the global database handle.
func (o *SRNewCopyright) SetSRG(insert bool, related *SynchronizationRaport) error {
	return o.SetSR(boil.GetDB(), insert, related)
}

// SetSRP of the sr_new_copyright to the related item.
// Sets o.R.SR to related.
// Adds o to related.R.SRSRNewCopyrights.
// Panics on error.
func (o *SRNewCopyright) SetSRP(exec boil.Executor, insert bool, related *SynchronizationRaport) {
	if err := o.SetSR(exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetSRGP of the sr_new_copyright to the related item.
// Sets o.R.SR to related.
// Adds o to related.R.SRSRNewCopyrights.
// Uses the global database handle and panics on error.
func (o *SRNewCopyright) SetSRGP(insert bool, related *SynchronizationRaport) {
	if err := o.SetSR(boil.GetDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetSR of the sr_new_copyright to the related item.
// Sets o.R.SR to related.
// Adds o to related.R.SRSRNewCopyrights.
func (o *SRNewCopyright) SetSR(exec boil.Executor, insert bool, related *SynchronizationRaport) error {
	var err error
	if insert {
		if err = related.Insert(exec); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `sr_new_copyrights` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"sr_id"}),
		strmangle.WhereClause("`", "`", 0, srNewCopyrightPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.SRID = related.ID

	if o.R == nil {
		o.R = &srNewCopyrightR{
			SR: related,
		}
	} else {
		o.R.SR = related
	}

	if related.R == nil {
		related.R = &synchronizationRaportR{
			SRSRNewCopyrights: SRNewCopyrightSlice{o},
		}
	} else {
		related.R.SRSRNewCopyrights = append(related.R.SRSRNewCopyrights, o)
	}

	return nil
}

// SetCopyrightG of the sr_new_copyright to the related item.
// Sets o.R.Copyright to related.
// Adds o to related.R.SRNewCopyrights.
// Uses the global database handle.
func (o *SRNewCopyright) SetCopyrightG(insert bool, related *Copyright) error {
	return o.SetCopyright(boil.GetDB(), insert, related)
}

// SetCopyrightP of the sr_new_copyright to the related item.
// Sets o.R.Copyright to related.
// Adds o to related.R.SRNewCopyrights.
// Panics on error.
func (o *SRNewCopyright) SetCopyrightP(exec boil.Executor, insert bool, related *Copyright) {
	if err := o.SetCopyright(exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetCopyrightGP of the sr_new_copyright to the related item.
// Sets o.R.Copyright to related.
// Adds o to related.R.SRNewCopyrights.
// Uses the global database handle and panics on error.
func (o *SRNewCopyright) SetCopyrightGP(insert bool, related *Copyright) {
	if err := o.SetCopyright(boil.GetDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetCopyright of the sr_new_copyright to the related item.
// Sets o.R.Copyright to related.
// Adds o to related.R.SRNewCopyrights.
func (o *SRNewCopyright) SetCopyright(exec boil.Executor, insert bool, related *Copyright) error {
	var err error
	if insert {
		if err = related.Insert(exec); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `sr_new_copyrights` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"copyright_id"}),
		strmangle.WhereClause("`", "`", 0, srNewCopyrightPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.CopyrightID = related.ID

	if o.R == nil {
		o.R = &srNewCopyrightR{
			Copyright: related,
		}
	} else {
		o.R.Copyright = related
	}

	if related.R == nil {
		related.R = &copyrightR{
			SRNewCopyrights: SRNewCopyrightSlice{o},
		}
	} else {
		related.R.SRNewCopyrights = append(related.R.SRNewCopyrights, o)
	}

	return nil
}

// SRNewCopyrightsG retrieves all records.
func SRNewCopyrightsG(mods ...qm.QueryMod) srNewCopyrightQuery {
	return SRNewCopyrights(boil.GetDB(), mods...)
}

// SRNewCopyrights retrieves all the records using an executor.
func SRNewCopyrights(exec boil.Executor, mods ...qm.QueryMod) srNewCopyrightQuery {
	mods = append(mods, qm.From("`sr_new_copyrights`"))
	return srNewCopyrightQuery{NewQuery(exec, mods...)}
}

// FindSRNewCopyrightG retrieves a single record by ID.
func FindSRNewCopyrightG(id uint64, selectCols ...string) (*SRNewCopyright, error) {
	return FindSRNewCopyright(boil.GetDB(), id, selectCols...)
}

// FindSRNewCopyrightGP retrieves a single record by ID, and panics on error.
func FindSRNewCopyrightGP(id uint64, selectCols ...string) *SRNewCopyright {
	retobj, err := FindSRNewCopyright(boil.GetDB(), id, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindSRNewCopyright retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindSRNewCopyright(exec boil.Executor, id uint64, selectCols ...string) (*SRNewCopyright, error) {
	srNewCopyrightObj := &SRNewCopyright{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `sr_new_copyrights` where `id`=?", sel,
	)

	q := queries.Raw(exec, query, id)

	err := q.Bind(srNewCopyrightObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from sr_new_copyrights")
	}

	return srNewCopyrightObj, nil
}

// FindSRNewCopyrightP retrieves a single record by ID with an executor, and panics on error.
func FindSRNewCopyrightP(exec boil.Executor, id uint64, selectCols ...string) *SRNewCopyright {
	retobj, err := FindSRNewCopyright(exec, id, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *SRNewCopyright) InsertG(whitelist ...string) error {
	return o.Insert(boil.GetDB(), whitelist...)
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *SRNewCopyright) InsertGP(whitelist ...string) {
	if err := o.Insert(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *SRNewCopyright) InsertP(exec boil.Executor, whitelist ...string) {
	if err := o.Insert(exec, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// Whitelist behavior: If a whitelist is provided, only those columns supplied are inserted
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns without a default value are included (i.e. name, age)
// - All columns with a default, but non-zero are included (i.e. health = 75)
func (o *SRNewCopyright) Insert(exec boil.Executor, whitelist ...string) error {
	if o == nil {
		return errors.New("models: no sr_new_copyrights provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(srNewCopyrightColumnsWithDefault, o)

	key := makeCacheKey(whitelist, nzDefaults)
	srNewCopyrightInsertCacheMut.RLock()
	cache, cached := srNewCopyrightInsertCache[key]
	srNewCopyrightInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := strmangle.InsertColumnSet(
			srNewCopyrightColumns,
			srNewCopyrightColumnsWithDefault,
			srNewCopyrightColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)

		cache.valueMapping, err = queries.BindMapping(srNewCopyrightType, srNewCopyrightMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(srNewCopyrightType, srNewCopyrightMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `sr_new_copyrights` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.IndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `sr_new_copyrights` () VALUES ()"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `sr_new_copyrights` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, srNewCopyrightPrimaryKeyColumns))
		}

		if len(wl) != 0 {
			cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.Exec(cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into sr_new_copyrights")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == srNewCopyrightMapping["ID"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, identifierCols...)
	}

	err = exec.QueryRow(cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for sr_new_copyrights")
	}

CacheNoHooks:
	if !cached {
		srNewCopyrightInsertCacheMut.Lock()
		srNewCopyrightInsertCache[key] = cache
		srNewCopyrightInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(exec)
}

// UpdateG a single SRNewCopyright record. See Update for
// whitelist behavior description.
func (o *SRNewCopyright) UpdateG(whitelist ...string) error {
	return o.Update(boil.GetDB(), whitelist...)
}

// UpdateGP a single SRNewCopyright record.
// UpdateGP takes a whitelist of column names that should be updated.
// Panics on error. See Update for whitelist behavior description.
func (o *SRNewCopyright) UpdateGP(whitelist ...string) {
	if err := o.Update(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateP uses an executor to update the SRNewCopyright, and panics on error.
// See Update for whitelist behavior description.
func (o *SRNewCopyright) UpdateP(exec boil.Executor, whitelist ...string) {
	err := o.Update(exec, whitelist...)
	if err != nil {
		panic(boil.WrapErr(err))
	}
}

// Update uses an executor to update the SRNewCopyright.
// Whitelist behavior: If a whitelist is provided, only the columns given are updated.
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns are inferred to start with
// - All primary keys are subtracted from this set
// Update does not automatically update the record in case of default values. Use .Reload()
// to refresh the records.
func (o *SRNewCopyright) Update(exec boil.Executor, whitelist ...string) error {
	var err error
	if err = o.doBeforeUpdateHooks(exec); err != nil {
		return err
	}
	key := makeCacheKey(whitelist, nil)
	srNewCopyrightUpdateCacheMut.RLock()
	cache, cached := srNewCopyrightUpdateCache[key]
	srNewCopyrightUpdateCacheMut.RUnlock()

	if !cached {
		wl := strmangle.UpdateColumnSet(
			srNewCopyrightColumns,
			srNewCopyrightPrimaryKeyColumns,
			whitelist,
		)

		if len(whitelist) == 0 {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return errors.New("models: unable to update sr_new_copyrights, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `sr_new_copyrights` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, srNewCopyrightPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(srNewCopyrightType, srNewCopyrightMapping, append(wl, srNewCopyrightPrimaryKeyColumns...))
		if err != nil {
			return err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	_, err = exec.Exec(cache.query, values...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update sr_new_copyrights row")
	}

	if !cached {
		srNewCopyrightUpdateCacheMut.Lock()
		srNewCopyrightUpdateCache[key] = cache
		srNewCopyrightUpdateCacheMut.Unlock()
	}

	return o.doAfterUpdateHooks(exec)
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q srNewCopyrightQuery) UpdateAllP(cols M) {
	if err := q.UpdateAll(cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values.
func (q srNewCopyrightQuery) UpdateAll(cols M) error {
	queries.SetUpdate(q.Query, cols)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "models: unable to update all for sr_new_copyrights")
	}

	return nil
}

// UpdateAllG updates all rows with the specified column values.
func (o SRNewCopyrightSlice) UpdateAllG(cols M) error {
	return o.UpdateAll(boil.GetDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o SRNewCopyrightSlice) UpdateAllGP(cols M) {
	if err := o.UpdateAll(boil.GetDB(), cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o SRNewCopyrightSlice) UpdateAllP(exec boil.Executor, cols M) {
	if err := o.UpdateAll(exec, cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o SRNewCopyrightSlice) UpdateAll(exec boil.Executor, cols M) error {
	ln := int64(len(o))
	if ln == 0 {
		return nil
	}

	if len(cols) == 0 {
		return errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), srNewCopyrightPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `sr_new_copyrights` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, srNewCopyrightPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update all in srNewCopyright slice")
	}

	return nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *SRNewCopyright) UpsertG(updateColumns []string, whitelist ...string) error {
	return o.Upsert(boil.GetDB(), updateColumns, whitelist...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *SRNewCopyright) UpsertGP(updateColumns []string, whitelist ...string) {
	if err := o.Upsert(boil.GetDB(), updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *SRNewCopyright) UpsertP(exec boil.Executor, updateColumns []string, whitelist ...string) {
	if err := o.Upsert(exec, updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
func (o *SRNewCopyright) Upsert(exec boil.Executor, updateColumns []string, whitelist ...string) error {
	if o == nil {
		return errors.New("models: no sr_new_copyrights provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(srNewCopyrightColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs postgres problems
	buf := strmangle.GetBuffer()
	for _, c := range updateColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range whitelist {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	srNewCopyrightUpsertCacheMut.RLock()
	cache, cached := srNewCopyrightUpsertCache[key]
	srNewCopyrightUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := strmangle.InsertColumnSet(
			srNewCopyrightColumns,
			srNewCopyrightColumnsWithDefault,
			srNewCopyrightColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)

		update := strmangle.UpdateColumnSet(
			srNewCopyrightColumns,
			srNewCopyrightPrimaryKeyColumns,
			updateColumns,
		)
		if len(update) == 0 {
			return errors.New("models: unable to upsert sr_new_copyrights, could not build update column list")
		}

		cache.query = queries.BuildUpsertQueryMySQL(dialect, "sr_new_copyrights", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `sr_new_copyrights` WHERE `id`=?",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
		)

		cache.valueMapping, err = queries.BindMapping(srNewCopyrightType, srNewCopyrightMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(srNewCopyrightType, srNewCopyrightMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.Exec(cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for sr_new_copyrights")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == srNewCopyrightMapping["ID"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, identifierCols...)
	}

	err = exec.QueryRow(cache.retQuery, identifierCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for sr_new_copyrights")
	}

CacheNoHooks:
	if !cached {
		srNewCopyrightUpsertCacheMut.Lock()
		srNewCopyrightUpsertCache[key] = cache
		srNewCopyrightUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(exec)
}

// DeleteP deletes a single SRNewCopyright record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *SRNewCopyright) DeleteP(exec boil.Executor) {
	if err := o.Delete(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteG deletes a single SRNewCopyright record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *SRNewCopyright) DeleteG() error {
	if o == nil {
		return errors.New("models: no SRNewCopyright provided for deletion")
	}

	return o.Delete(boil.GetDB())
}

// DeleteGP deletes a single SRNewCopyright record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *SRNewCopyright) DeleteGP() {
	if err := o.DeleteG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Delete deletes a single SRNewCopyright record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *SRNewCopyright) Delete(exec boil.Executor) error {
	if o == nil {
		return errors.New("models: no SRNewCopyright provided for delete")
	}

	if err := o.doBeforeDeleteHooks(exec); err != nil {
		return err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), srNewCopyrightPrimaryKeyMapping)
	sql := "DELETE FROM `sr_new_copyrights` WHERE `id`=?"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete from sr_new_copyrights")
	}

	if err := o.doAfterDeleteHooks(exec); err != nil {
		return err
	}

	return nil
}

// DeleteAllP deletes all rows, and panics on error.
func (q srNewCopyrightQuery) DeleteAllP() {
	if err := q.DeleteAll(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all matching rows.
func (q srNewCopyrightQuery) DeleteAll() error {
	if q.Query == nil {
		return errors.New("models: no srNewCopyrightQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from sr_new_copyrights")
	}

	return nil
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o SRNewCopyrightSlice) DeleteAllGP() {
	if err := o.DeleteAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAllG deletes all rows in the slice.
func (o SRNewCopyrightSlice) DeleteAllG() error {
	if o == nil {
		return errors.New("models: no SRNewCopyright slice provided for delete all")
	}
	return o.DeleteAll(boil.GetDB())
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o SRNewCopyrightSlice) DeleteAllP(exec boil.Executor) {
	if err := o.DeleteAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o SRNewCopyrightSlice) DeleteAll(exec boil.Executor) error {
	if o == nil {
		return errors.New("models: no SRNewCopyright slice provided for delete all")
	}

	if len(o) == 0 {
		return nil
	}

	if len(srNewCopyrightBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(exec); err != nil {
				return err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), srNewCopyrightPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `sr_new_copyrights` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, srNewCopyrightPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from srNewCopyright slice")
	}

	if len(srNewCopyrightAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(exec); err != nil {
				return err
			}
		}
	}

	return nil
}

// ReloadGP refetches the object from the database and panics on error.
func (o *SRNewCopyright) ReloadGP() {
	if err := o.ReloadG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *SRNewCopyright) ReloadP(exec boil.Executor) {
	if err := o.Reload(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadG refetches the object from the database using the primary keys.
func (o *SRNewCopyright) ReloadG() error {
	if o == nil {
		return errors.New("models: no SRNewCopyright provided for reload")
	}

	return o.Reload(boil.GetDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *SRNewCopyright) Reload(exec boil.Executor) error {
	ret, err := FindSRNewCopyright(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *SRNewCopyrightSlice) ReloadAllGP() {
	if err := o.ReloadAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *SRNewCopyrightSlice) ReloadAllP(exec boil.Executor) {
	if err := o.ReloadAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *SRNewCopyrightSlice) ReloadAllG() error {
	if o == nil {
		return errors.New("models: empty SRNewCopyrightSlice provided for reload all")
	}

	return o.ReloadAll(boil.GetDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *SRNewCopyrightSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	srNewCopyrights := SRNewCopyrightSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), srNewCopyrightPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `sr_new_copyrights`.* FROM `sr_new_copyrights` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, srNewCopyrightPrimaryKeyColumns, len(*o))

	q := queries.Raw(exec, sql, args...)

	err := q.Bind(&srNewCopyrights)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in SRNewCopyrightSlice")
	}

	*o = srNewCopyrights

	return nil
}

// SRNewCopyrightExists checks if the SRNewCopyright row exists.
func SRNewCopyrightExists(exec boil.Executor, id uint64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `sr_new_copyrights` where `id`=? limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, id)
	}

	row := exec.QueryRow(sql, id)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if sr_new_copyrights exists")
	}

	return exists, nil
}

// SRNewCopyrightExistsG checks if the SRNewCopyright row exists.
func SRNewCopyrightExistsG(id uint64) (bool, error) {
	return SRNewCopyrightExists(boil.GetDB(), id)
}

// SRNewCopyrightExistsGP checks if the SRNewCopyright row exists. Panics on error.
func SRNewCopyrightExistsGP(id uint64) bool {
	e, err := SRNewCopyrightExists(boil.GetDB(), id)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// SRNewCopyrightExistsP checks if the SRNewCopyright row exists. Panics on error.
func SRNewCopyrightExistsP(exec boil.Executor, id uint64) bool {
	e, err := SRNewCopyrightExists(exec, id)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}
