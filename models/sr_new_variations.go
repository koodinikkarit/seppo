// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"database/sql"
	"fmt"
	"reflect"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// SRNewVariation is an object representing the database table.
type SRNewVariation struct {
	ID          uint64 `boil:"id" json:"id" toml:"id" yaml:"id"`
	SRID        uint64 `boil:"sr_id" json:"sr_id" toml:"sr_id" yaml:"sr_id"`
	VariationID uint64 `boil:"variation_id" json:"variation_id" toml:"variation_id" yaml:"variation_id"`

	R *srNewVariationR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L srNewVariationL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var SRNewVariationColumns = struct {
	ID          string
	SRID        string
	VariationID string
}{
	ID:          "id",
	SRID:        "sr_id",
	VariationID: "variation_id",
}

// srNewVariationR is where relationships are stored.
type srNewVariationR struct {
	SR        *SynchronizationRaport
	Variation *Variation
}

// srNewVariationL is where Load methods for each relationship are stored.
type srNewVariationL struct{}

var (
	srNewVariationColumns               = []string{"id", "sr_id", "variation_id"}
	srNewVariationColumnsWithoutDefault = []string{"sr_id", "variation_id"}
	srNewVariationColumnsWithDefault    = []string{"id"}
	srNewVariationPrimaryKeyColumns     = []string{"id"}
)

type (
	// SRNewVariationSlice is an alias for a slice of pointers to SRNewVariation.
	// This should generally be used opposed to []SRNewVariation.
	SRNewVariationSlice []*SRNewVariation
	// SRNewVariationHook is the signature for custom SRNewVariation hook methods
	SRNewVariationHook func(boil.Executor, *SRNewVariation) error

	srNewVariationQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	srNewVariationType                 = reflect.TypeOf(&SRNewVariation{})
	srNewVariationMapping              = queries.MakeStructMapping(srNewVariationType)
	srNewVariationPrimaryKeyMapping, _ = queries.BindMapping(srNewVariationType, srNewVariationMapping, srNewVariationPrimaryKeyColumns)
	srNewVariationInsertCacheMut       sync.RWMutex
	srNewVariationInsertCache          = make(map[string]insertCache)
	srNewVariationUpdateCacheMut       sync.RWMutex
	srNewVariationUpdateCache          = make(map[string]updateCache)
	srNewVariationUpsertCacheMut       sync.RWMutex
	srNewVariationUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force bytes in case of primary key column that uses []byte (for relationship compares)
	_ = bytes.MinRead
)
var srNewVariationBeforeInsertHooks []SRNewVariationHook
var srNewVariationBeforeUpdateHooks []SRNewVariationHook
var srNewVariationBeforeDeleteHooks []SRNewVariationHook
var srNewVariationBeforeUpsertHooks []SRNewVariationHook

var srNewVariationAfterInsertHooks []SRNewVariationHook
var srNewVariationAfterSelectHooks []SRNewVariationHook
var srNewVariationAfterUpdateHooks []SRNewVariationHook
var srNewVariationAfterDeleteHooks []SRNewVariationHook
var srNewVariationAfterUpsertHooks []SRNewVariationHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *SRNewVariation) doBeforeInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range srNewVariationBeforeInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *SRNewVariation) doBeforeUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range srNewVariationBeforeUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *SRNewVariation) doBeforeDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range srNewVariationBeforeDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *SRNewVariation) doBeforeUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range srNewVariationBeforeUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *SRNewVariation) doAfterInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range srNewVariationAfterInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *SRNewVariation) doAfterSelectHooks(exec boil.Executor) (err error) {
	for _, hook := range srNewVariationAfterSelectHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *SRNewVariation) doAfterUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range srNewVariationAfterUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *SRNewVariation) doAfterDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range srNewVariationAfterDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *SRNewVariation) doAfterUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range srNewVariationAfterUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddSRNewVariationHook registers your hook function for all future operations.
func AddSRNewVariationHook(hookPoint boil.HookPoint, srNewVariationHook SRNewVariationHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		srNewVariationBeforeInsertHooks = append(srNewVariationBeforeInsertHooks, srNewVariationHook)
	case boil.BeforeUpdateHook:
		srNewVariationBeforeUpdateHooks = append(srNewVariationBeforeUpdateHooks, srNewVariationHook)
	case boil.BeforeDeleteHook:
		srNewVariationBeforeDeleteHooks = append(srNewVariationBeforeDeleteHooks, srNewVariationHook)
	case boil.BeforeUpsertHook:
		srNewVariationBeforeUpsertHooks = append(srNewVariationBeforeUpsertHooks, srNewVariationHook)
	case boil.AfterInsertHook:
		srNewVariationAfterInsertHooks = append(srNewVariationAfterInsertHooks, srNewVariationHook)
	case boil.AfterSelectHook:
		srNewVariationAfterSelectHooks = append(srNewVariationAfterSelectHooks, srNewVariationHook)
	case boil.AfterUpdateHook:
		srNewVariationAfterUpdateHooks = append(srNewVariationAfterUpdateHooks, srNewVariationHook)
	case boil.AfterDeleteHook:
		srNewVariationAfterDeleteHooks = append(srNewVariationAfterDeleteHooks, srNewVariationHook)
	case boil.AfterUpsertHook:
		srNewVariationAfterUpsertHooks = append(srNewVariationAfterUpsertHooks, srNewVariationHook)
	}
}

// OneP returns a single srNewVariation record from the query, and panics on error.
func (q srNewVariationQuery) OneP() *SRNewVariation {
	o, err := q.One()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single srNewVariation record from the query.
func (q srNewVariationQuery) One() (*SRNewVariation, error) {
	o := &SRNewVariation{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for sr_new_variations")
	}

	if err := o.doAfterSelectHooks(queries.GetExecutor(q.Query)); err != nil {
		return o, err
	}

	return o, nil
}

// AllP returns all SRNewVariation records from the query, and panics on error.
func (q srNewVariationQuery) AllP() SRNewVariationSlice {
	o, err := q.All()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all SRNewVariation records from the query.
func (q srNewVariationQuery) All() (SRNewVariationSlice, error) {
	var o []*SRNewVariation

	err := q.Bind(&o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to SRNewVariation slice")
	}

	if len(srNewVariationAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(queries.GetExecutor(q.Query)); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountP returns the count of all SRNewVariation records in the query, and panics on error.
func (q srNewVariationQuery) CountP() int64 {
	c, err := q.Count()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all SRNewVariation records in the query.
func (q srNewVariationQuery) Count() (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count sr_new_variations rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table, and panics on error.
func (q srNewVariationQuery) ExistsP() bool {
	e, err := q.Exists()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q srNewVariationQuery) Exists() (bool, error) {
	var count int64

	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if sr_new_variations exists")
	}

	return count > 0, nil
}

// SRG pointed to by the foreign key.
func (o *SRNewVariation) SRG(mods ...qm.QueryMod) synchronizationRaportQuery {
	return o.SR(boil.GetDB(), mods...)
}

// SR pointed to by the foreign key.
func (o *SRNewVariation) SR(exec boil.Executor, mods ...qm.QueryMod) synchronizationRaportQuery {
	queryMods := []qm.QueryMod{
		qm.Where("id=?", o.SRID),
	}

	queryMods = append(queryMods, mods...)

	query := SynchronizationRaports(exec, queryMods...)
	queries.SetFrom(query.Query, "`synchronization_raports`")

	return query
}

// VariationG pointed to by the foreign key.
func (o *SRNewVariation) VariationG(mods ...qm.QueryMod) variationQuery {
	return o.Variation(boil.GetDB(), mods...)
}

// Variation pointed to by the foreign key.
func (o *SRNewVariation) Variation(exec boil.Executor, mods ...qm.QueryMod) variationQuery {
	queryMods := []qm.QueryMod{
		qm.Where("id=?", o.VariationID),
	}

	queryMods = append(queryMods, mods...)

	query := Variations(exec, queryMods...)
	queries.SetFrom(query.Query, "`variations`")

	return query
} // LoadSR allows an eager lookup of values, cached into the
// loaded structs of the objects.
func (srNewVariationL) LoadSR(e boil.Executor, singular bool, maybeSRNewVariation interface{}) error {
	var slice []*SRNewVariation
	var object *SRNewVariation

	count := 1
	if singular {
		object = maybeSRNewVariation.(*SRNewVariation)
	} else {
		slice = *maybeSRNewVariation.(*[]*SRNewVariation)
		count = len(slice)
	}

	args := make([]interface{}, count)
	if singular {
		if object.R == nil {
			object.R = &srNewVariationR{}
		}
		args[0] = object.SRID
	} else {
		for i, obj := range slice {
			if obj.R == nil {
				obj.R = &srNewVariationR{}
			}
			args[i] = obj.SRID
		}
	}

	query := fmt.Sprintf(
		"select * from `synchronization_raports` where `id` in (%s)",
		strmangle.Placeholders(dialect.IndexPlaceholders, count, 1, 1),
	)

	if boil.DebugMode {
		fmt.Fprintf(boil.DebugWriter, "%s\n%v\n", query, args)
	}

	results, err := e.Query(query, args...)
	if err != nil {
		return errors.Wrap(err, "failed to eager load SynchronizationRaport")
	}
	defer results.Close()

	var resultSlice []*SynchronizationRaport
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice SynchronizationRaport")
	}

	if len(srNewVariationAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		object.R.SR = resultSlice[0]
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.SRID == foreign.ID {
				local.R.SR = foreign
				break
			}
		}
	}

	return nil
}

// LoadVariation allows an eager lookup of values, cached into the
// loaded structs of the objects.
func (srNewVariationL) LoadVariation(e boil.Executor, singular bool, maybeSRNewVariation interface{}) error {
	var slice []*SRNewVariation
	var object *SRNewVariation

	count := 1
	if singular {
		object = maybeSRNewVariation.(*SRNewVariation)
	} else {
		slice = *maybeSRNewVariation.(*[]*SRNewVariation)
		count = len(slice)
	}

	args := make([]interface{}, count)
	if singular {
		if object.R == nil {
			object.R = &srNewVariationR{}
		}
		args[0] = object.VariationID
	} else {
		for i, obj := range slice {
			if obj.R == nil {
				obj.R = &srNewVariationR{}
			}
			args[i] = obj.VariationID
		}
	}

	query := fmt.Sprintf(
		"select * from `variations` where `id` in (%s)",
		strmangle.Placeholders(dialect.IndexPlaceholders, count, 1, 1),
	)

	if boil.DebugMode {
		fmt.Fprintf(boil.DebugWriter, "%s\n%v\n", query, args)
	}

	results, err := e.Query(query, args...)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Variation")
	}
	defer results.Close()

	var resultSlice []*Variation
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Variation")
	}

	if len(srNewVariationAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		object.R.Variation = resultSlice[0]
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.VariationID == foreign.ID {
				local.R.Variation = foreign
				break
			}
		}
	}

	return nil
}

// SetSRG of the sr_new_variation to the related item.
// Sets o.R.SR to related.
// Adds o to related.R.SRSRNewVariations.
// Uses the global database handle.
func (o *SRNewVariation) SetSRG(insert bool, related *SynchronizationRaport) error {
	return o.SetSR(boil.GetDB(), insert, related)
}

// SetSRP of the sr_new_variation to the related item.
// Sets o.R.SR to related.
// Adds o to related.R.SRSRNewVariations.
// Panics on error.
func (o *SRNewVariation) SetSRP(exec boil.Executor, insert bool, related *SynchronizationRaport) {
	if err := o.SetSR(exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetSRGP of the sr_new_variation to the related item.
// Sets o.R.SR to related.
// Adds o to related.R.SRSRNewVariations.
// Uses the global database handle and panics on error.
func (o *SRNewVariation) SetSRGP(insert bool, related *SynchronizationRaport) {
	if err := o.SetSR(boil.GetDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetSR of the sr_new_variation to the related item.
// Sets o.R.SR to related.
// Adds o to related.R.SRSRNewVariations.
func (o *SRNewVariation) SetSR(exec boil.Executor, insert bool, related *SynchronizationRaport) error {
	var err error
	if insert {
		if err = related.Insert(exec); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `sr_new_variations` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"sr_id"}),
		strmangle.WhereClause("`", "`", 0, srNewVariationPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.SRID = related.ID

	if o.R == nil {
		o.R = &srNewVariationR{
			SR: related,
		}
	} else {
		o.R.SR = related
	}

	if related.R == nil {
		related.R = &synchronizationRaportR{
			SRSRNewVariations: SRNewVariationSlice{o},
		}
	} else {
		related.R.SRSRNewVariations = append(related.R.SRSRNewVariations, o)
	}

	return nil
}

// SetVariationG of the sr_new_variation to the related item.
// Sets o.R.Variation to related.
// Adds o to related.R.SRNewVariations.
// Uses the global database handle.
func (o *SRNewVariation) SetVariationG(insert bool, related *Variation) error {
	return o.SetVariation(boil.GetDB(), insert, related)
}

// SetVariationP of the sr_new_variation to the related item.
// Sets o.R.Variation to related.
// Adds o to related.R.SRNewVariations.
// Panics on error.
func (o *SRNewVariation) SetVariationP(exec boil.Executor, insert bool, related *Variation) {
	if err := o.SetVariation(exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetVariationGP of the sr_new_variation to the related item.
// Sets o.R.Variation to related.
// Adds o to related.R.SRNewVariations.
// Uses the global database handle and panics on error.
func (o *SRNewVariation) SetVariationGP(insert bool, related *Variation) {
	if err := o.SetVariation(boil.GetDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetVariation of the sr_new_variation to the related item.
// Sets o.R.Variation to related.
// Adds o to related.R.SRNewVariations.
func (o *SRNewVariation) SetVariation(exec boil.Executor, insert bool, related *Variation) error {
	var err error
	if insert {
		if err = related.Insert(exec); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `sr_new_variations` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"variation_id"}),
		strmangle.WhereClause("`", "`", 0, srNewVariationPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.VariationID = related.ID

	if o.R == nil {
		o.R = &srNewVariationR{
			Variation: related,
		}
	} else {
		o.R.Variation = related
	}

	if related.R == nil {
		related.R = &variationR{
			SRNewVariations: SRNewVariationSlice{o},
		}
	} else {
		related.R.SRNewVariations = append(related.R.SRNewVariations, o)
	}

	return nil
}

// SRNewVariationsG retrieves all records.
func SRNewVariationsG(mods ...qm.QueryMod) srNewVariationQuery {
	return SRNewVariations(boil.GetDB(), mods...)
}

// SRNewVariations retrieves all the records using an executor.
func SRNewVariations(exec boil.Executor, mods ...qm.QueryMod) srNewVariationQuery {
	mods = append(mods, qm.From("`sr_new_variations`"))
	return srNewVariationQuery{NewQuery(exec, mods...)}
}

// FindSRNewVariationG retrieves a single record by ID.
func FindSRNewVariationG(id uint64, selectCols ...string) (*SRNewVariation, error) {
	return FindSRNewVariation(boil.GetDB(), id, selectCols...)
}

// FindSRNewVariationGP retrieves a single record by ID, and panics on error.
func FindSRNewVariationGP(id uint64, selectCols ...string) *SRNewVariation {
	retobj, err := FindSRNewVariation(boil.GetDB(), id, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindSRNewVariation retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindSRNewVariation(exec boil.Executor, id uint64, selectCols ...string) (*SRNewVariation, error) {
	srNewVariationObj := &SRNewVariation{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `sr_new_variations` where `id`=?", sel,
	)

	q := queries.Raw(exec, query, id)

	err := q.Bind(srNewVariationObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from sr_new_variations")
	}

	return srNewVariationObj, nil
}

// FindSRNewVariationP retrieves a single record by ID with an executor, and panics on error.
func FindSRNewVariationP(exec boil.Executor, id uint64, selectCols ...string) *SRNewVariation {
	retobj, err := FindSRNewVariation(exec, id, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *SRNewVariation) InsertG(whitelist ...string) error {
	return o.Insert(boil.GetDB(), whitelist...)
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *SRNewVariation) InsertGP(whitelist ...string) {
	if err := o.Insert(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *SRNewVariation) InsertP(exec boil.Executor, whitelist ...string) {
	if err := o.Insert(exec, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// Whitelist behavior: If a whitelist is provided, only those columns supplied are inserted
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns without a default value are included (i.e. name, age)
// - All columns with a default, but non-zero are included (i.e. health = 75)
func (o *SRNewVariation) Insert(exec boil.Executor, whitelist ...string) error {
	if o == nil {
		return errors.New("models: no sr_new_variations provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(srNewVariationColumnsWithDefault, o)

	key := makeCacheKey(whitelist, nzDefaults)
	srNewVariationInsertCacheMut.RLock()
	cache, cached := srNewVariationInsertCache[key]
	srNewVariationInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := strmangle.InsertColumnSet(
			srNewVariationColumns,
			srNewVariationColumnsWithDefault,
			srNewVariationColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)

		cache.valueMapping, err = queries.BindMapping(srNewVariationType, srNewVariationMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(srNewVariationType, srNewVariationMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `sr_new_variations` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.IndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `sr_new_variations` () VALUES ()"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `sr_new_variations` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, srNewVariationPrimaryKeyColumns))
		}

		if len(wl) != 0 {
			cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.Exec(cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into sr_new_variations")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == srNewVariationMapping["ID"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, identifierCols...)
	}

	err = exec.QueryRow(cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for sr_new_variations")
	}

CacheNoHooks:
	if !cached {
		srNewVariationInsertCacheMut.Lock()
		srNewVariationInsertCache[key] = cache
		srNewVariationInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(exec)
}

// UpdateG a single SRNewVariation record. See Update for
// whitelist behavior description.
func (o *SRNewVariation) UpdateG(whitelist ...string) error {
	return o.Update(boil.GetDB(), whitelist...)
}

// UpdateGP a single SRNewVariation record.
// UpdateGP takes a whitelist of column names that should be updated.
// Panics on error. See Update for whitelist behavior description.
func (o *SRNewVariation) UpdateGP(whitelist ...string) {
	if err := o.Update(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateP uses an executor to update the SRNewVariation, and panics on error.
// See Update for whitelist behavior description.
func (o *SRNewVariation) UpdateP(exec boil.Executor, whitelist ...string) {
	err := o.Update(exec, whitelist...)
	if err != nil {
		panic(boil.WrapErr(err))
	}
}

// Update uses an executor to update the SRNewVariation.
// Whitelist behavior: If a whitelist is provided, only the columns given are updated.
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns are inferred to start with
// - All primary keys are subtracted from this set
// Update does not automatically update the record in case of default values. Use .Reload()
// to refresh the records.
func (o *SRNewVariation) Update(exec boil.Executor, whitelist ...string) error {
	var err error
	if err = o.doBeforeUpdateHooks(exec); err != nil {
		return err
	}
	key := makeCacheKey(whitelist, nil)
	srNewVariationUpdateCacheMut.RLock()
	cache, cached := srNewVariationUpdateCache[key]
	srNewVariationUpdateCacheMut.RUnlock()

	if !cached {
		wl := strmangle.UpdateColumnSet(
			srNewVariationColumns,
			srNewVariationPrimaryKeyColumns,
			whitelist,
		)

		if len(whitelist) == 0 {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return errors.New("models: unable to update sr_new_variations, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `sr_new_variations` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, srNewVariationPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(srNewVariationType, srNewVariationMapping, append(wl, srNewVariationPrimaryKeyColumns...))
		if err != nil {
			return err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	_, err = exec.Exec(cache.query, values...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update sr_new_variations row")
	}

	if !cached {
		srNewVariationUpdateCacheMut.Lock()
		srNewVariationUpdateCache[key] = cache
		srNewVariationUpdateCacheMut.Unlock()
	}

	return o.doAfterUpdateHooks(exec)
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q srNewVariationQuery) UpdateAllP(cols M) {
	if err := q.UpdateAll(cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values.
func (q srNewVariationQuery) UpdateAll(cols M) error {
	queries.SetUpdate(q.Query, cols)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "models: unable to update all for sr_new_variations")
	}

	return nil
}

// UpdateAllG updates all rows with the specified column values.
func (o SRNewVariationSlice) UpdateAllG(cols M) error {
	return o.UpdateAll(boil.GetDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o SRNewVariationSlice) UpdateAllGP(cols M) {
	if err := o.UpdateAll(boil.GetDB(), cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o SRNewVariationSlice) UpdateAllP(exec boil.Executor, cols M) {
	if err := o.UpdateAll(exec, cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o SRNewVariationSlice) UpdateAll(exec boil.Executor, cols M) error {
	ln := int64(len(o))
	if ln == 0 {
		return nil
	}

	if len(cols) == 0 {
		return errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), srNewVariationPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `sr_new_variations` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, srNewVariationPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update all in srNewVariation slice")
	}

	return nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *SRNewVariation) UpsertG(updateColumns []string, whitelist ...string) error {
	return o.Upsert(boil.GetDB(), updateColumns, whitelist...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *SRNewVariation) UpsertGP(updateColumns []string, whitelist ...string) {
	if err := o.Upsert(boil.GetDB(), updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *SRNewVariation) UpsertP(exec boil.Executor, updateColumns []string, whitelist ...string) {
	if err := o.Upsert(exec, updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
func (o *SRNewVariation) Upsert(exec boil.Executor, updateColumns []string, whitelist ...string) error {
	if o == nil {
		return errors.New("models: no sr_new_variations provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(srNewVariationColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs postgres problems
	buf := strmangle.GetBuffer()
	for _, c := range updateColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range whitelist {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	srNewVariationUpsertCacheMut.RLock()
	cache, cached := srNewVariationUpsertCache[key]
	srNewVariationUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := strmangle.InsertColumnSet(
			srNewVariationColumns,
			srNewVariationColumnsWithDefault,
			srNewVariationColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)

		update := strmangle.UpdateColumnSet(
			srNewVariationColumns,
			srNewVariationPrimaryKeyColumns,
			updateColumns,
		)
		if len(update) == 0 {
			return errors.New("models: unable to upsert sr_new_variations, could not build update column list")
		}

		cache.query = queries.BuildUpsertQueryMySQL(dialect, "sr_new_variations", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `sr_new_variations` WHERE `id`=?",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
		)

		cache.valueMapping, err = queries.BindMapping(srNewVariationType, srNewVariationMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(srNewVariationType, srNewVariationMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.Exec(cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for sr_new_variations")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == srNewVariationMapping["ID"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, identifierCols...)
	}

	err = exec.QueryRow(cache.retQuery, identifierCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for sr_new_variations")
	}

CacheNoHooks:
	if !cached {
		srNewVariationUpsertCacheMut.Lock()
		srNewVariationUpsertCache[key] = cache
		srNewVariationUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(exec)
}

// DeleteP deletes a single SRNewVariation record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *SRNewVariation) DeleteP(exec boil.Executor) {
	if err := o.Delete(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteG deletes a single SRNewVariation record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *SRNewVariation) DeleteG() error {
	if o == nil {
		return errors.New("models: no SRNewVariation provided for deletion")
	}

	return o.Delete(boil.GetDB())
}

// DeleteGP deletes a single SRNewVariation record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *SRNewVariation) DeleteGP() {
	if err := o.DeleteG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Delete deletes a single SRNewVariation record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *SRNewVariation) Delete(exec boil.Executor) error {
	if o == nil {
		return errors.New("models: no SRNewVariation provided for delete")
	}

	if err := o.doBeforeDeleteHooks(exec); err != nil {
		return err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), srNewVariationPrimaryKeyMapping)
	sql := "DELETE FROM `sr_new_variations` WHERE `id`=?"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete from sr_new_variations")
	}

	if err := o.doAfterDeleteHooks(exec); err != nil {
		return err
	}

	return nil
}

// DeleteAllP deletes all rows, and panics on error.
func (q srNewVariationQuery) DeleteAllP() {
	if err := q.DeleteAll(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all matching rows.
func (q srNewVariationQuery) DeleteAll() error {
	if q.Query == nil {
		return errors.New("models: no srNewVariationQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from sr_new_variations")
	}

	return nil
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o SRNewVariationSlice) DeleteAllGP() {
	if err := o.DeleteAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAllG deletes all rows in the slice.
func (o SRNewVariationSlice) DeleteAllG() error {
	if o == nil {
		return errors.New("models: no SRNewVariation slice provided for delete all")
	}
	return o.DeleteAll(boil.GetDB())
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o SRNewVariationSlice) DeleteAllP(exec boil.Executor) {
	if err := o.DeleteAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o SRNewVariationSlice) DeleteAll(exec boil.Executor) error {
	if o == nil {
		return errors.New("models: no SRNewVariation slice provided for delete all")
	}

	if len(o) == 0 {
		return nil
	}

	if len(srNewVariationBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(exec); err != nil {
				return err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), srNewVariationPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `sr_new_variations` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, srNewVariationPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from srNewVariation slice")
	}

	if len(srNewVariationAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(exec); err != nil {
				return err
			}
		}
	}

	return nil
}

// ReloadGP refetches the object from the database and panics on error.
func (o *SRNewVariation) ReloadGP() {
	if err := o.ReloadG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *SRNewVariation) ReloadP(exec boil.Executor) {
	if err := o.Reload(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadG refetches the object from the database using the primary keys.
func (o *SRNewVariation) ReloadG() error {
	if o == nil {
		return errors.New("models: no SRNewVariation provided for reload")
	}

	return o.Reload(boil.GetDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *SRNewVariation) Reload(exec boil.Executor) error {
	ret, err := FindSRNewVariation(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *SRNewVariationSlice) ReloadAllGP() {
	if err := o.ReloadAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *SRNewVariationSlice) ReloadAllP(exec boil.Executor) {
	if err := o.ReloadAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *SRNewVariationSlice) ReloadAllG() error {
	if o == nil {
		return errors.New("models: empty SRNewVariationSlice provided for reload all")
	}

	return o.ReloadAll(boil.GetDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *SRNewVariationSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	srNewVariations := SRNewVariationSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), srNewVariationPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `sr_new_variations`.* FROM `sr_new_variations` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, srNewVariationPrimaryKeyColumns, len(*o))

	q := queries.Raw(exec, sql, args...)

	err := q.Bind(&srNewVariations)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in SRNewVariationSlice")
	}

	*o = srNewVariations

	return nil
}

// SRNewVariationExists checks if the SRNewVariation row exists.
func SRNewVariationExists(exec boil.Executor, id uint64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `sr_new_variations` where `id`=? limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, id)
	}

	row := exec.QueryRow(sql, id)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if sr_new_variations exists")
	}

	return exists, nil
}

// SRNewVariationExistsG checks if the SRNewVariation row exists.
func SRNewVariationExistsG(id uint64) (bool, error) {
	return SRNewVariationExists(boil.GetDB(), id)
}

// SRNewVariationExistsGP checks if the SRNewVariation row exists. Panics on error.
func SRNewVariationExistsGP(id uint64) bool {
	e, err := SRNewVariationExists(boil.GetDB(), id)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// SRNewVariationExistsP checks if the SRNewVariation row exists. Panics on error.
func SRNewVariationExistsP(exec boil.Executor, id uint64) bool {
	e, err := SRNewVariationExists(exec, id)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}
