// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"database/sql"
	"fmt"
	"reflect"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// SRNewAuthor is an object representing the database table.
type SRNewAuthor struct {
	ID       uint64 `boil:"id" json:"id" toml:"id" yaml:"id"`
	SRID     uint64 `boil:"sr_id" json:"sr_id" toml:"sr_id" yaml:"sr_id"`
	AuthorID uint64 `boil:"author_id" json:"author_id" toml:"author_id" yaml:"author_id"`

	R *srNewAuthorR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L srNewAuthorL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var SRNewAuthorColumns = struct {
	ID       string
	SRID     string
	AuthorID string
}{
	ID:       "id",
	SRID:     "sr_id",
	AuthorID: "author_id",
}

// srNewAuthorR is where relationships are stored.
type srNewAuthorR struct {
	SR     *SynchronizationRaport
	Author *Author
}

// srNewAuthorL is where Load methods for each relationship are stored.
type srNewAuthorL struct{}

var (
	srNewAuthorColumns               = []string{"id", "sr_id", "author_id"}
	srNewAuthorColumnsWithoutDefault = []string{"sr_id", "author_id"}
	srNewAuthorColumnsWithDefault    = []string{"id"}
	srNewAuthorPrimaryKeyColumns     = []string{"id"}
)

type (
	// SRNewAuthorSlice is an alias for a slice of pointers to SRNewAuthor.
	// This should generally be used opposed to []SRNewAuthor.
	SRNewAuthorSlice []*SRNewAuthor
	// SRNewAuthorHook is the signature for custom SRNewAuthor hook methods
	SRNewAuthorHook func(boil.Executor, *SRNewAuthor) error

	srNewAuthorQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	srNewAuthorType                 = reflect.TypeOf(&SRNewAuthor{})
	srNewAuthorMapping              = queries.MakeStructMapping(srNewAuthorType)
	srNewAuthorPrimaryKeyMapping, _ = queries.BindMapping(srNewAuthorType, srNewAuthorMapping, srNewAuthorPrimaryKeyColumns)
	srNewAuthorInsertCacheMut       sync.RWMutex
	srNewAuthorInsertCache          = make(map[string]insertCache)
	srNewAuthorUpdateCacheMut       sync.RWMutex
	srNewAuthorUpdateCache          = make(map[string]updateCache)
	srNewAuthorUpsertCacheMut       sync.RWMutex
	srNewAuthorUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force bytes in case of primary key column that uses []byte (for relationship compares)
	_ = bytes.MinRead
)
var srNewAuthorBeforeInsertHooks []SRNewAuthorHook
var srNewAuthorBeforeUpdateHooks []SRNewAuthorHook
var srNewAuthorBeforeDeleteHooks []SRNewAuthorHook
var srNewAuthorBeforeUpsertHooks []SRNewAuthorHook

var srNewAuthorAfterInsertHooks []SRNewAuthorHook
var srNewAuthorAfterSelectHooks []SRNewAuthorHook
var srNewAuthorAfterUpdateHooks []SRNewAuthorHook
var srNewAuthorAfterDeleteHooks []SRNewAuthorHook
var srNewAuthorAfterUpsertHooks []SRNewAuthorHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *SRNewAuthor) doBeforeInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range srNewAuthorBeforeInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *SRNewAuthor) doBeforeUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range srNewAuthorBeforeUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *SRNewAuthor) doBeforeDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range srNewAuthorBeforeDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *SRNewAuthor) doBeforeUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range srNewAuthorBeforeUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *SRNewAuthor) doAfterInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range srNewAuthorAfterInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *SRNewAuthor) doAfterSelectHooks(exec boil.Executor) (err error) {
	for _, hook := range srNewAuthorAfterSelectHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *SRNewAuthor) doAfterUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range srNewAuthorAfterUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *SRNewAuthor) doAfterDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range srNewAuthorAfterDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *SRNewAuthor) doAfterUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range srNewAuthorAfterUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddSRNewAuthorHook registers your hook function for all future operations.
func AddSRNewAuthorHook(hookPoint boil.HookPoint, srNewAuthorHook SRNewAuthorHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		srNewAuthorBeforeInsertHooks = append(srNewAuthorBeforeInsertHooks, srNewAuthorHook)
	case boil.BeforeUpdateHook:
		srNewAuthorBeforeUpdateHooks = append(srNewAuthorBeforeUpdateHooks, srNewAuthorHook)
	case boil.BeforeDeleteHook:
		srNewAuthorBeforeDeleteHooks = append(srNewAuthorBeforeDeleteHooks, srNewAuthorHook)
	case boil.BeforeUpsertHook:
		srNewAuthorBeforeUpsertHooks = append(srNewAuthorBeforeUpsertHooks, srNewAuthorHook)
	case boil.AfterInsertHook:
		srNewAuthorAfterInsertHooks = append(srNewAuthorAfterInsertHooks, srNewAuthorHook)
	case boil.AfterSelectHook:
		srNewAuthorAfterSelectHooks = append(srNewAuthorAfterSelectHooks, srNewAuthorHook)
	case boil.AfterUpdateHook:
		srNewAuthorAfterUpdateHooks = append(srNewAuthorAfterUpdateHooks, srNewAuthorHook)
	case boil.AfterDeleteHook:
		srNewAuthorAfterDeleteHooks = append(srNewAuthorAfterDeleteHooks, srNewAuthorHook)
	case boil.AfterUpsertHook:
		srNewAuthorAfterUpsertHooks = append(srNewAuthorAfterUpsertHooks, srNewAuthorHook)
	}
}

// OneP returns a single srNewAuthor record from the query, and panics on error.
func (q srNewAuthorQuery) OneP() *SRNewAuthor {
	o, err := q.One()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single srNewAuthor record from the query.
func (q srNewAuthorQuery) One() (*SRNewAuthor, error) {
	o := &SRNewAuthor{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for sr_new_authors")
	}

	if err := o.doAfterSelectHooks(queries.GetExecutor(q.Query)); err != nil {
		return o, err
	}

	return o, nil
}

// AllP returns all SRNewAuthor records from the query, and panics on error.
func (q srNewAuthorQuery) AllP() SRNewAuthorSlice {
	o, err := q.All()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all SRNewAuthor records from the query.
func (q srNewAuthorQuery) All() (SRNewAuthorSlice, error) {
	var o []*SRNewAuthor

	err := q.Bind(&o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to SRNewAuthor slice")
	}

	if len(srNewAuthorAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(queries.GetExecutor(q.Query)); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountP returns the count of all SRNewAuthor records in the query, and panics on error.
func (q srNewAuthorQuery) CountP() int64 {
	c, err := q.Count()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all SRNewAuthor records in the query.
func (q srNewAuthorQuery) Count() (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count sr_new_authors rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table, and panics on error.
func (q srNewAuthorQuery) ExistsP() bool {
	e, err := q.Exists()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q srNewAuthorQuery) Exists() (bool, error) {
	var count int64

	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if sr_new_authors exists")
	}

	return count > 0, nil
}

// SRG pointed to by the foreign key.
func (o *SRNewAuthor) SRG(mods ...qm.QueryMod) synchronizationRaportQuery {
	return o.SR(boil.GetDB(), mods...)
}

// SR pointed to by the foreign key.
func (o *SRNewAuthor) SR(exec boil.Executor, mods ...qm.QueryMod) synchronizationRaportQuery {
	queryMods := []qm.QueryMod{
		qm.Where("id=?", o.SRID),
	}

	queryMods = append(queryMods, mods...)

	query := SynchronizationRaports(exec, queryMods...)
	queries.SetFrom(query.Query, "`synchronization_raports`")

	return query
}

// AuthorG pointed to by the foreign key.
func (o *SRNewAuthor) AuthorG(mods ...qm.QueryMod) authorQuery {
	return o.Author(boil.GetDB(), mods...)
}

// Author pointed to by the foreign key.
func (o *SRNewAuthor) Author(exec boil.Executor, mods ...qm.QueryMod) authorQuery {
	queryMods := []qm.QueryMod{
		qm.Where("id=?", o.AuthorID),
	}

	queryMods = append(queryMods, mods...)

	query := Authors(exec, queryMods...)
	queries.SetFrom(query.Query, "`authors`")

	return query
} // LoadSR allows an eager lookup of values, cached into the
// loaded structs of the objects.
func (srNewAuthorL) LoadSR(e boil.Executor, singular bool, maybeSRNewAuthor interface{}) error {
	var slice []*SRNewAuthor
	var object *SRNewAuthor

	count := 1
	if singular {
		object = maybeSRNewAuthor.(*SRNewAuthor)
	} else {
		slice = *maybeSRNewAuthor.(*[]*SRNewAuthor)
		count = len(slice)
	}

	args := make([]interface{}, count)
	if singular {
		if object.R == nil {
			object.R = &srNewAuthorR{}
		}
		args[0] = object.SRID
	} else {
		for i, obj := range slice {
			if obj.R == nil {
				obj.R = &srNewAuthorR{}
			}
			args[i] = obj.SRID
		}
	}

	query := fmt.Sprintf(
		"select * from `synchronization_raports` where `id` in (%s)",
		strmangle.Placeholders(dialect.IndexPlaceholders, count, 1, 1),
	)

	if boil.DebugMode {
		fmt.Fprintf(boil.DebugWriter, "%s\n%v\n", query, args)
	}

	results, err := e.Query(query, args...)
	if err != nil {
		return errors.Wrap(err, "failed to eager load SynchronizationRaport")
	}
	defer results.Close()

	var resultSlice []*SynchronizationRaport
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice SynchronizationRaport")
	}

	if len(srNewAuthorAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		object.R.SR = resultSlice[0]
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.SRID == foreign.ID {
				local.R.SR = foreign
				break
			}
		}
	}

	return nil
}

// LoadAuthor allows an eager lookup of values, cached into the
// loaded structs of the objects.
func (srNewAuthorL) LoadAuthor(e boil.Executor, singular bool, maybeSRNewAuthor interface{}) error {
	var slice []*SRNewAuthor
	var object *SRNewAuthor

	count := 1
	if singular {
		object = maybeSRNewAuthor.(*SRNewAuthor)
	} else {
		slice = *maybeSRNewAuthor.(*[]*SRNewAuthor)
		count = len(slice)
	}

	args := make([]interface{}, count)
	if singular {
		if object.R == nil {
			object.R = &srNewAuthorR{}
		}
		args[0] = object.AuthorID
	} else {
		for i, obj := range slice {
			if obj.R == nil {
				obj.R = &srNewAuthorR{}
			}
			args[i] = obj.AuthorID
		}
	}

	query := fmt.Sprintf(
		"select * from `authors` where `id` in (%s)",
		strmangle.Placeholders(dialect.IndexPlaceholders, count, 1, 1),
	)

	if boil.DebugMode {
		fmt.Fprintf(boil.DebugWriter, "%s\n%v\n", query, args)
	}

	results, err := e.Query(query, args...)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Author")
	}
	defer results.Close()

	var resultSlice []*Author
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Author")
	}

	if len(srNewAuthorAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		object.R.Author = resultSlice[0]
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.AuthorID == foreign.ID {
				local.R.Author = foreign
				break
			}
		}
	}

	return nil
}

// SetSRG of the sr_new_author to the related item.
// Sets o.R.SR to related.
// Adds o to related.R.SRSRNewAuthors.
// Uses the global database handle.
func (o *SRNewAuthor) SetSRG(insert bool, related *SynchronizationRaport) error {
	return o.SetSR(boil.GetDB(), insert, related)
}

// SetSRP of the sr_new_author to the related item.
// Sets o.R.SR to related.
// Adds o to related.R.SRSRNewAuthors.
// Panics on error.
func (o *SRNewAuthor) SetSRP(exec boil.Executor, insert bool, related *SynchronizationRaport) {
	if err := o.SetSR(exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetSRGP of the sr_new_author to the related item.
// Sets o.R.SR to related.
// Adds o to related.R.SRSRNewAuthors.
// Uses the global database handle and panics on error.
func (o *SRNewAuthor) SetSRGP(insert bool, related *SynchronizationRaport) {
	if err := o.SetSR(boil.GetDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetSR of the sr_new_author to the related item.
// Sets o.R.SR to related.
// Adds o to related.R.SRSRNewAuthors.
func (o *SRNewAuthor) SetSR(exec boil.Executor, insert bool, related *SynchronizationRaport) error {
	var err error
	if insert {
		if err = related.Insert(exec); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `sr_new_authors` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"sr_id"}),
		strmangle.WhereClause("`", "`", 0, srNewAuthorPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.SRID = related.ID

	if o.R == nil {
		o.R = &srNewAuthorR{
			SR: related,
		}
	} else {
		o.R.SR = related
	}

	if related.R == nil {
		related.R = &synchronizationRaportR{
			SRSRNewAuthors: SRNewAuthorSlice{o},
		}
	} else {
		related.R.SRSRNewAuthors = append(related.R.SRSRNewAuthors, o)
	}

	return nil
}

// SetAuthorG of the sr_new_author to the related item.
// Sets o.R.Author to related.
// Adds o to related.R.SRNewAuthors.
// Uses the global database handle.
func (o *SRNewAuthor) SetAuthorG(insert bool, related *Author) error {
	return o.SetAuthor(boil.GetDB(), insert, related)
}

// SetAuthorP of the sr_new_author to the related item.
// Sets o.R.Author to related.
// Adds o to related.R.SRNewAuthors.
// Panics on error.
func (o *SRNewAuthor) SetAuthorP(exec boil.Executor, insert bool, related *Author) {
	if err := o.SetAuthor(exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetAuthorGP of the sr_new_author to the related item.
// Sets o.R.Author to related.
// Adds o to related.R.SRNewAuthors.
// Uses the global database handle and panics on error.
func (o *SRNewAuthor) SetAuthorGP(insert bool, related *Author) {
	if err := o.SetAuthor(boil.GetDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetAuthor of the sr_new_author to the related item.
// Sets o.R.Author to related.
// Adds o to related.R.SRNewAuthors.
func (o *SRNewAuthor) SetAuthor(exec boil.Executor, insert bool, related *Author) error {
	var err error
	if insert {
		if err = related.Insert(exec); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `sr_new_authors` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"author_id"}),
		strmangle.WhereClause("`", "`", 0, srNewAuthorPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.AuthorID = related.ID

	if o.R == nil {
		o.R = &srNewAuthorR{
			Author: related,
		}
	} else {
		o.R.Author = related
	}

	if related.R == nil {
		related.R = &authorR{
			SRNewAuthors: SRNewAuthorSlice{o},
		}
	} else {
		related.R.SRNewAuthors = append(related.R.SRNewAuthors, o)
	}

	return nil
}

// SRNewAuthorsG retrieves all records.
func SRNewAuthorsG(mods ...qm.QueryMod) srNewAuthorQuery {
	return SRNewAuthors(boil.GetDB(), mods...)
}

// SRNewAuthors retrieves all the records using an executor.
func SRNewAuthors(exec boil.Executor, mods ...qm.QueryMod) srNewAuthorQuery {
	mods = append(mods, qm.From("`sr_new_authors`"))
	return srNewAuthorQuery{NewQuery(exec, mods...)}
}

// FindSRNewAuthorG retrieves a single record by ID.
func FindSRNewAuthorG(id uint64, selectCols ...string) (*SRNewAuthor, error) {
	return FindSRNewAuthor(boil.GetDB(), id, selectCols...)
}

// FindSRNewAuthorGP retrieves a single record by ID, and panics on error.
func FindSRNewAuthorGP(id uint64, selectCols ...string) *SRNewAuthor {
	retobj, err := FindSRNewAuthor(boil.GetDB(), id, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindSRNewAuthor retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindSRNewAuthor(exec boil.Executor, id uint64, selectCols ...string) (*SRNewAuthor, error) {
	srNewAuthorObj := &SRNewAuthor{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `sr_new_authors` where `id`=?", sel,
	)

	q := queries.Raw(exec, query, id)

	err := q.Bind(srNewAuthorObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from sr_new_authors")
	}

	return srNewAuthorObj, nil
}

// FindSRNewAuthorP retrieves a single record by ID with an executor, and panics on error.
func FindSRNewAuthorP(exec boil.Executor, id uint64, selectCols ...string) *SRNewAuthor {
	retobj, err := FindSRNewAuthor(exec, id, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *SRNewAuthor) InsertG(whitelist ...string) error {
	return o.Insert(boil.GetDB(), whitelist...)
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *SRNewAuthor) InsertGP(whitelist ...string) {
	if err := o.Insert(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *SRNewAuthor) InsertP(exec boil.Executor, whitelist ...string) {
	if err := o.Insert(exec, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// Whitelist behavior: If a whitelist is provided, only those columns supplied are inserted
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns without a default value are included (i.e. name, age)
// - All columns with a default, but non-zero are included (i.e. health = 75)
func (o *SRNewAuthor) Insert(exec boil.Executor, whitelist ...string) error {
	if o == nil {
		return errors.New("models: no sr_new_authors provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(srNewAuthorColumnsWithDefault, o)

	key := makeCacheKey(whitelist, nzDefaults)
	srNewAuthorInsertCacheMut.RLock()
	cache, cached := srNewAuthorInsertCache[key]
	srNewAuthorInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := strmangle.InsertColumnSet(
			srNewAuthorColumns,
			srNewAuthorColumnsWithDefault,
			srNewAuthorColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)

		cache.valueMapping, err = queries.BindMapping(srNewAuthorType, srNewAuthorMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(srNewAuthorType, srNewAuthorMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `sr_new_authors` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.IndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `sr_new_authors` () VALUES ()"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `sr_new_authors` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, srNewAuthorPrimaryKeyColumns))
		}

		if len(wl) != 0 {
			cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.Exec(cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into sr_new_authors")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == srNewAuthorMapping["ID"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, identifierCols...)
	}

	err = exec.QueryRow(cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for sr_new_authors")
	}

CacheNoHooks:
	if !cached {
		srNewAuthorInsertCacheMut.Lock()
		srNewAuthorInsertCache[key] = cache
		srNewAuthorInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(exec)
}

// UpdateG a single SRNewAuthor record. See Update for
// whitelist behavior description.
func (o *SRNewAuthor) UpdateG(whitelist ...string) error {
	return o.Update(boil.GetDB(), whitelist...)
}

// UpdateGP a single SRNewAuthor record.
// UpdateGP takes a whitelist of column names that should be updated.
// Panics on error. See Update for whitelist behavior description.
func (o *SRNewAuthor) UpdateGP(whitelist ...string) {
	if err := o.Update(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateP uses an executor to update the SRNewAuthor, and panics on error.
// See Update for whitelist behavior description.
func (o *SRNewAuthor) UpdateP(exec boil.Executor, whitelist ...string) {
	err := o.Update(exec, whitelist...)
	if err != nil {
		panic(boil.WrapErr(err))
	}
}

// Update uses an executor to update the SRNewAuthor.
// Whitelist behavior: If a whitelist is provided, only the columns given are updated.
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns are inferred to start with
// - All primary keys are subtracted from this set
// Update does not automatically update the record in case of default values. Use .Reload()
// to refresh the records.
func (o *SRNewAuthor) Update(exec boil.Executor, whitelist ...string) error {
	var err error
	if err = o.doBeforeUpdateHooks(exec); err != nil {
		return err
	}
	key := makeCacheKey(whitelist, nil)
	srNewAuthorUpdateCacheMut.RLock()
	cache, cached := srNewAuthorUpdateCache[key]
	srNewAuthorUpdateCacheMut.RUnlock()

	if !cached {
		wl := strmangle.UpdateColumnSet(
			srNewAuthorColumns,
			srNewAuthorPrimaryKeyColumns,
			whitelist,
		)

		if len(whitelist) == 0 {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return errors.New("models: unable to update sr_new_authors, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `sr_new_authors` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, srNewAuthorPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(srNewAuthorType, srNewAuthorMapping, append(wl, srNewAuthorPrimaryKeyColumns...))
		if err != nil {
			return err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	_, err = exec.Exec(cache.query, values...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update sr_new_authors row")
	}

	if !cached {
		srNewAuthorUpdateCacheMut.Lock()
		srNewAuthorUpdateCache[key] = cache
		srNewAuthorUpdateCacheMut.Unlock()
	}

	return o.doAfterUpdateHooks(exec)
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q srNewAuthorQuery) UpdateAllP(cols M) {
	if err := q.UpdateAll(cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values.
func (q srNewAuthorQuery) UpdateAll(cols M) error {
	queries.SetUpdate(q.Query, cols)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "models: unable to update all for sr_new_authors")
	}

	return nil
}

// UpdateAllG updates all rows with the specified column values.
func (o SRNewAuthorSlice) UpdateAllG(cols M) error {
	return o.UpdateAll(boil.GetDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o SRNewAuthorSlice) UpdateAllGP(cols M) {
	if err := o.UpdateAll(boil.GetDB(), cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o SRNewAuthorSlice) UpdateAllP(exec boil.Executor, cols M) {
	if err := o.UpdateAll(exec, cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o SRNewAuthorSlice) UpdateAll(exec boil.Executor, cols M) error {
	ln := int64(len(o))
	if ln == 0 {
		return nil
	}

	if len(cols) == 0 {
		return errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), srNewAuthorPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `sr_new_authors` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, srNewAuthorPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update all in srNewAuthor slice")
	}

	return nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *SRNewAuthor) UpsertG(updateColumns []string, whitelist ...string) error {
	return o.Upsert(boil.GetDB(), updateColumns, whitelist...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *SRNewAuthor) UpsertGP(updateColumns []string, whitelist ...string) {
	if err := o.Upsert(boil.GetDB(), updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *SRNewAuthor) UpsertP(exec boil.Executor, updateColumns []string, whitelist ...string) {
	if err := o.Upsert(exec, updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
func (o *SRNewAuthor) Upsert(exec boil.Executor, updateColumns []string, whitelist ...string) error {
	if o == nil {
		return errors.New("models: no sr_new_authors provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(srNewAuthorColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs postgres problems
	buf := strmangle.GetBuffer()
	for _, c := range updateColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range whitelist {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	srNewAuthorUpsertCacheMut.RLock()
	cache, cached := srNewAuthorUpsertCache[key]
	srNewAuthorUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := strmangle.InsertColumnSet(
			srNewAuthorColumns,
			srNewAuthorColumnsWithDefault,
			srNewAuthorColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)

		update := strmangle.UpdateColumnSet(
			srNewAuthorColumns,
			srNewAuthorPrimaryKeyColumns,
			updateColumns,
		)
		if len(update) == 0 {
			return errors.New("models: unable to upsert sr_new_authors, could not build update column list")
		}

		cache.query = queries.BuildUpsertQueryMySQL(dialect, "sr_new_authors", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `sr_new_authors` WHERE `id`=?",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
		)

		cache.valueMapping, err = queries.BindMapping(srNewAuthorType, srNewAuthorMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(srNewAuthorType, srNewAuthorMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.Exec(cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for sr_new_authors")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == srNewAuthorMapping["ID"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, identifierCols...)
	}

	err = exec.QueryRow(cache.retQuery, identifierCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for sr_new_authors")
	}

CacheNoHooks:
	if !cached {
		srNewAuthorUpsertCacheMut.Lock()
		srNewAuthorUpsertCache[key] = cache
		srNewAuthorUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(exec)
}

// DeleteP deletes a single SRNewAuthor record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *SRNewAuthor) DeleteP(exec boil.Executor) {
	if err := o.Delete(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteG deletes a single SRNewAuthor record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *SRNewAuthor) DeleteG() error {
	if o == nil {
		return errors.New("models: no SRNewAuthor provided for deletion")
	}

	return o.Delete(boil.GetDB())
}

// DeleteGP deletes a single SRNewAuthor record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *SRNewAuthor) DeleteGP() {
	if err := o.DeleteG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Delete deletes a single SRNewAuthor record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *SRNewAuthor) Delete(exec boil.Executor) error {
	if o == nil {
		return errors.New("models: no SRNewAuthor provided for delete")
	}

	if err := o.doBeforeDeleteHooks(exec); err != nil {
		return err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), srNewAuthorPrimaryKeyMapping)
	sql := "DELETE FROM `sr_new_authors` WHERE `id`=?"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete from sr_new_authors")
	}

	if err := o.doAfterDeleteHooks(exec); err != nil {
		return err
	}

	return nil
}

// DeleteAllP deletes all rows, and panics on error.
func (q srNewAuthorQuery) DeleteAllP() {
	if err := q.DeleteAll(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all matching rows.
func (q srNewAuthorQuery) DeleteAll() error {
	if q.Query == nil {
		return errors.New("models: no srNewAuthorQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from sr_new_authors")
	}

	return nil
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o SRNewAuthorSlice) DeleteAllGP() {
	if err := o.DeleteAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAllG deletes all rows in the slice.
func (o SRNewAuthorSlice) DeleteAllG() error {
	if o == nil {
		return errors.New("models: no SRNewAuthor slice provided for delete all")
	}
	return o.DeleteAll(boil.GetDB())
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o SRNewAuthorSlice) DeleteAllP(exec boil.Executor) {
	if err := o.DeleteAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o SRNewAuthorSlice) DeleteAll(exec boil.Executor) error {
	if o == nil {
		return errors.New("models: no SRNewAuthor slice provided for delete all")
	}

	if len(o) == 0 {
		return nil
	}

	if len(srNewAuthorBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(exec); err != nil {
				return err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), srNewAuthorPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `sr_new_authors` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, srNewAuthorPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from srNewAuthor slice")
	}

	if len(srNewAuthorAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(exec); err != nil {
				return err
			}
		}
	}

	return nil
}

// ReloadGP refetches the object from the database and panics on error.
func (o *SRNewAuthor) ReloadGP() {
	if err := o.ReloadG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *SRNewAuthor) ReloadP(exec boil.Executor) {
	if err := o.Reload(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadG refetches the object from the database using the primary keys.
func (o *SRNewAuthor) ReloadG() error {
	if o == nil {
		return errors.New("models: no SRNewAuthor provided for reload")
	}

	return o.Reload(boil.GetDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *SRNewAuthor) Reload(exec boil.Executor) error {
	ret, err := FindSRNewAuthor(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *SRNewAuthorSlice) ReloadAllGP() {
	if err := o.ReloadAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *SRNewAuthorSlice) ReloadAllP(exec boil.Executor) {
	if err := o.ReloadAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *SRNewAuthorSlice) ReloadAllG() error {
	if o == nil {
		return errors.New("models: empty SRNewAuthorSlice provided for reload all")
	}

	return o.ReloadAll(boil.GetDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *SRNewAuthorSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	srNewAuthors := SRNewAuthorSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), srNewAuthorPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `sr_new_authors`.* FROM `sr_new_authors` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, srNewAuthorPrimaryKeyColumns, len(*o))

	q := queries.Raw(exec, sql, args...)

	err := q.Bind(&srNewAuthors)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in SRNewAuthorSlice")
	}

	*o = srNewAuthors

	return nil
}

// SRNewAuthorExists checks if the SRNewAuthor row exists.
func SRNewAuthorExists(exec boil.Executor, id uint64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `sr_new_authors` where `id`=? limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, id)
	}

	row := exec.QueryRow(sql, id)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if sr_new_authors exists")
	}

	return exists, nil
}

// SRNewAuthorExistsG checks if the SRNewAuthor row exists.
func SRNewAuthorExistsG(id uint64) (bool, error) {
	return SRNewAuthorExists(boil.GetDB(), id)
}

// SRNewAuthorExistsGP checks if the SRNewAuthor row exists. Panics on error.
func SRNewAuthorExistsGP(id uint64) bool {
	e, err := SRNewAuthorExists(boil.GetDB(), id)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// SRNewAuthorExistsP checks if the SRNewAuthor row exists. Panics on error.
func SRNewAuthorExistsP(exec boil.Executor, id uint64) bool {
	e, err := SRNewAuthorExists(exec, id)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}
